---
id: B-0002
type: bug
title: `asf new` cannot type the fields a card needs, so every card must be rewritten after minting
parent: E-0001
priority: need
decided: true
severity: S2
found_in: dev
signature: "new: typed fields missing"
area: factory
# ---- machine ----
state: Active
stage_since: 2026-09-21T19:04:23Z
evidence:
  - "PR #3 OPEN"
  - branch fix/B-0002
updated: 2026-09-21T19:04:24Z
cost: {sessions: 1, fix_rounds: 1, runner_min: 0, usd: null}
---
## Description
`asf new` accepts only `--title`, `--parent`, `--priority`, `--area`, `--legacy-id` and `--body-file`. A card in this schema also carries `rank`, `decided`, `source`, `severity`, `found_in`, `signature`, `scope`, `enforced`, `reason`, `check`, `decided_by`, `date`, `blockedBy` and `links`. Every one of the cards in this record was minted with `new` and then re-rendered by a script, which means the one tool that is supposed to own card creation owns only the id.

## Fix
`new` takes every typed field of the schema, or takes them as `--set key=value` pairs validated against the type's field list, so a card is complete when `new` returns.

## History
- 2026-09-21 seed: filed while seeding this record by hand, because `init` does not exist
- 2026-09-21 19:04 ingest: state New → Active (PR #3 OPEN; branch fix/B-0002)

## Children

## Backlinks
