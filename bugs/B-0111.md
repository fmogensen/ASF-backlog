---
id: B-0111
type: bug
title: Inbox intake drops headers after an unknown key; a Bug card became a Feature
parent: E-0001
decided: true
severity: S2
found_in: dev
signature: "\"inbox intake stops reading headers at the first unknown key, and silently drops the rest (signature: included)\""
# ---- machine ----
schema_version: 1
state: Resolved
stage_since: 2026-09-25T17:35:24Z
evidence:
  - merge 85bd8c9 of fix/B-0111 lands B-0111
  - "rule: fixed"
updated: 2026-09-25T17:35:24Z
cost: {sessions: 12, fix_rounds: 1, runner_min: 0, usd: 11.76, tokens_input: 808, tokens_output: 215589, tokens_cache_read: 23827026, tokens_cache_write: 600581}
---
## Description
## Description
- `groom/inbox.py` reads a card's header lines with `INBOX_KV_RE` (type|parent|signature|severity|writes|stories), and the first line that does not match ends the header block.
- A card whose headers were `parent:`, `after:`, `signature:`, `severity:` lost everything after `after:`. The signature was never read, the shape fell to `default`, and a defect became a decided Feature. Under a feature hold, that means it would never launch.
- The same thing happened this morning with `type: bug` placed after the header. Twice in one day, the cause was a header line intake does not know about, dropped without a word.

## Expected
- Intake either accepts `after:` (it is a valid item field) or refuses the card, naming the unknown header line. It never drops the header lines that follow one it doesn't know.
- A test: a card with an unknown header line before `signature:` is refused or shaped as a Bug, never as a Feature.

## Acceptance
- [ ] 

## Non-goals

## History
- 2026-09-24: created (inbox) — shape: signature → bug
- 2026-09-24 groom: decided → true (adjudicator, groom-2026-09-24)
- 2026-09-24 22:47 ingest: state New → Active (rule: fixer; branch fix/B-0111)
- 2026-09-25 14:39 adjudicate (adjudicate-b-0111): What was disputed was never the code — round 3 already resolved both regressions rounds 1 and 2 found (`asf/groom/inbox.py`'s `_UNREAD_HEADER_KEYS` registry correctly drops `after:` whether or not a further confirmed header follows it, and `INBOX_KV_RE` matches keep advancing `body_start` regardless of position, so a header-shaped body line is never mistaken for a header and a real header is never mistaken for body) and was approved with a fully green gate. The STALEMATE fired because the branch was held a third time with `kind='gate'` (`asf/feeder/rows.py:368`'s `held {rounds} times ({kind})` reads back as `held 3 times (gate)`) — a harvest-time product-gate refusal *after* the content review had already approved, not a fourth content defect. Rounds 1 and 2's own reviews independently recorded the same host at load average 27-50 causing `tools/run_tests.py --shards 4` to be killed or time out before completing (`docs/reviews/1-b-0111.md` I3, `docs/reviews/2-b-0111.md`'s Gate row); round 3 only went green once load dropped to 5-9. `asf.harvest.harvest.product_gate` kills a gate past `gate_timeout_s` (default 600s, B-0072) and that refusal is handed back through `send_back`/`hold_with_correction` exactly like a real red, consuming a round — so a host-contention timeout and a genuine defect are indistinguishable to the round counter, and this item paid for the former as if it were the latter. I re-ran every gate command myself on this exact head just now, at load average 3-11: `check_generic.sh` clean, `check_conventions.sh` clean (replicated — its real invocation is sandbox-blocked in this session too), and the full suite green in 311s (2881 tests, OK, 93 modules). The gate hold is overruled — checked against `docs/reviews/1-b-0111.md` and `2-b-0111.md`'s own load-average evidence and against a fresh green run on this head; it does not come back in a later round. The only change made: `git rm .run_tests_gate.log`, the zero-byte stray file I2 flagged unresolved across all three rounds — harmless, but there is no round after this one to carry it forward in. The branch is ready to land as `8856796`.
- 2026-09-25 15:34 adjudicate (adjudicate-b-0111): Disputed: the STALEMATE → ADJUDICATE hold ("held 3 times (gate): adjudicate, not another correction") and round 1-4's carried I1 ("no plan and no spec for an S2 bug"). Both overruled. The gate hold: I checked the branch's actual lineage against `docs/reviews/1-4-b-0111.md` and the diff itself — rounds 1 and 2 each carried a genuinely gate-breaking head (round 1: an unrecognized header line dropped real headers after it; round 2's own fix for that introduced a fresh leak, a trailing `after:` line bleeding into the description), each closed by the next round's fix, not by wishful correction. The current head is unchanged since round 3's approval (`git diff d54eeed..HEAD -- asf/groom/inbox.py tests/test_inbox_shape.py` is empty) — round 3 ran the full declared gate to completion green (`tools/run_tests.py --shards 4`: 2834 tests OK), round 4 ran it again green (2884 tests OK), and I independently re-ran the module suite green a third time. There is no open C finding on the branch and nothing left in the code for a fourth correction session to find — exactly the "another correction will not converge" case adjudication exists for. The I1 dispute: `asf/feeder/rows.py:303` names a Bug's `## Fix` as its own plan — only Feature cards route through `docs/specs/`/`docs/plans/` (`CARD → SPEC`, `asf/feeder/rows.py:505-509`). The "acceptance tests byte-identical to the plan's" row failing every round is a checklist row that does not apply to Bug cards, not a real gap; it was correctly never treated as blocking (verdict stayed "approved" through it). What changes: the hold is lifted, the branch lands as reviewed with no further session; the card's footprint, never declared through 4 rounds, is set explicitly below so a future footprint gate has a real fence instead of none.
- 2026-09-25 16:31 adjudicate (adjudicate-b-0111): Disputed was the review table's "acceptance tests byte-identical to the plan's" row, which read fail in rounds 1-4 solely because docs/plans/b-0111.md and docs/specs/b-0111.md were never written for this card — every round's own notes called that gap "not the writer's to fix from inside this branch," yet the gate held on it repeatedly even after rounds 3 and 4 both approved the actual fix with zero open C findings. I checked the code myself rather than trust the table: asf/groom/inbox.py's `_UNREAD_HEADER_KEYS` registry (inbox.py:17-18, 88-89) drops `after:` wherever it sits and never swallows a body line that merely looks header-shaped, confirmed live against the card's own repro (`parent:`/`after:`/`signature:`/`severity:` all read correctly now, the signature is no longer lost, `type: bug` placed after other headers is no longer dropped); the full test suite and gate are green, matching what I ran above. I checked whether a plan/spec is actually this item's convention: zero of any bug-type card in this repo carries a docs/specs/*.md file, only 3 of many carry a docs/plans/*.md, and nothing in asf/ enforces either as a precondition for a bug fix landing — the review table's row is a generic template check applied to an item class it was never built for, not a real gap in this fix. I overrule that row as a blocking condition for B-0111: the fix is correct, scoped, and fully tested, and writing a plan/spec after the fact for a bug already fixed and twice-approved would be paperwork, not verification. Nothing else is open on this branch — I2 (the stray zero-byte gate log) is already closed by commit 6bd4bb9, and rounds 3-4 both closed with zero C findings. The branch is ready to land as-is; this overruled row does not come back in a later round.
- 2026-09-25 17:07 adjudicate (adjudicate-b-0111): Disputed across all four review rounds (docs/reviews/1-b-0111.md:56-61, 2-b-0111.md:58-61, 3-b-0111.md:29-33, 4-b-0111.md:29-31) was whether B-0111 can land without `docs/specs/b-0111.md` / `docs/plans/b-0111.md`, each round flagging their absence (I1) as unresolved and explicitly "not the writer's to fix from inside this branch." I overrule I1 as a blocker: round 1 itself records this as "the same gap the B-0109 and B-0099 round-1 reviews noted for their cards" (docs/reviews/1-b-0111.md:56-58) — a structural pattern for small bug cards in this system, not a defect this branch introduced — and the branch-prefix convention (`spec/`, `plan/` vs. `fix/`) already keeps spec/plan authorship outside a `fix/` branch's scope; the card's own Acceptance is a single empty checkbox, so there is nothing a plan would have fenced anyway. On the substance: round 1's C1 (an unrecognized header-shaped line eating the next body line, `asf/groom/inbox.py:79-85` at the time) and round 2's C1 (a trailing, unconfirmed `after:` leaking into the description) are both fixed — the current code names `_UNREAD_HEADER_KEYS = ('after',)` explicitly (`asf/groom/inbox.py:17-18`) rather than matching shape alone, closing both the header-drop the card describes and the two regressions review found along the way. Three regression tests (`tests/test_inbox_shape.py:400`, `:412`, `:427`) cover exactly the two real incidents the card's Description names (a recognized header stranded after `after:`, and `type: bug` stranded the same way) plus both review-found regressions. Round 3 and round 4 both recorded verdict `approved` with zero C-findings, and I reconfirmed the gate green just now. Nothing here needed a fifth correction round; the branch lands as it stands.
- 2026-09-25 17:35 ingest: state Active → Resolved (rule: fixed; merge 85bd8c9 of fix/B-0111 lands B-0111)

## Children

## Backlinks
