---
id: B-0001
type: bug
title: `asf init` does not exist, so a new record has to be laid down by hand
parent: E-0001
priority: need
decided: true
severity: S2
found_in: dev
signature: "init: command missing"
area: factory
# ---- machine ----
state: Closed
stage_since: 2026-09-21T21:06:04Z
evidence:
  - commit c5307be names B-0001
  - CI green on main at or after it
updated: 2026-09-21T21:06:04Z
---
## Description
The plan gives `asf init` as the adopter that creates a product's record in twenty minutes. The CLI's subcommand list is `new check index ingest migrate groom stale file-bugs rules evidence harvest pr-hygiene`; there is no `init`. This record's folder layout, its README, its pre-commit hook and its empty index were all written by hand.

## Fix
`asf init --product <name>` creates the folder layout (the seven item folders plus inbox, groom, releases and metrics streams), writes the schema README, installs the pre-commit hook, writes an empty index and prints the first roadmap.

## History
- 2026-09-21 seed: filed while seeding this record by hand, because `init` does not exist
- 2026-09-21 17:20 ingest: state New → Resolved (commit c5307be names B-0001)
- 2026-09-21 21:06 ingest: state Resolved → Closed (commit c5307be names B-0001; CI green on main at or after it)

## Children

## Backlinks
- [F-0072](../features/F-0072.md) Install and upgrade: pipx package, asf init adopts, schema check on every write, asf migrate, PATH-bound scheduler and hooks
- [B-0020](../bugs/B-0020.md) Evidence is blind to any product but the first: branch prefixes hardcoded, id tokens in commits ignored
- [B-0027](../bugs/B-0027.md) Nothing lands a lane branch on a product without a merge queue: harvest reads the wrong repo, prefix and key, and is not a step
- [B-0030](../bugs/B-0030.md) A record push refused because origin moved is discarded; the tick's appended events are lost
- [B-0036](../bugs/B-0036.md) A landing never reaches the checkout the scheduler runs; the factory keeps running the code from before its own fix
