---
id: B-0025
type: bug
title: A session that ended with nothing committed leaves a worktree that blocks every relaunch
parent: F-0015
priority: need
decided: true
area: factory
severity: S2
found_in: dev
signature: "spawn: dead empty worktree blocks relaunch"
removed: "superseded: the empty/landed reap rule is on main (fix(B-0049), F-0087) — operator ruling 2026-09-22"
# ---- machine ----
schema_version: 1
state: Closed
stage_since: 2026-09-22T09:03:52Z
cost: {sessions: 5, fix_rounds: 2, runner_min: 0, usd: 11.89}
evidence:
  - commit 7c8c756 names B-0025
  - CI green on main at or after it
  - "rule: quiet"
updated: 2026-09-25T09:23:08Z
---
## Description
A session that ended without committing anything (it died at launch, or found nothing to do and did not push) leaves its worktree under `state/<p>/worktrees/<job>`; health keeps it (the reap rule requires a pushed, landed branch), and the next wave's `spawn` refuses `worktree already exists` — so the Bug is never retried. Seen on 2026-09-21 after the first wave died on an unmapped model label: B-0003 and B-0008 could not relaunch.

## Fix
Health reaps a worktree whose session is `ended`, whose pid is dead, and whose branch has zero commits ahead of the base (nothing can be lost) — the complement of B-0019 (a LIVE session or a branch WITH commits is never reaped). Spawn, meeting an existing worktree with a dead session and zero commits, removes it and proceeds; with commits, it refuses with the branch name and the commit count so a human can look. Tests for all three cases.

## History
- 2026-09-21 19:33 the second wave: two S1 Bugs skipped with `spawn failed: worktree already exists`
- 2026-09-21 20:50 — every tick since re-emits B-0003/B-0008 as `waits … spawn failed: worktree already exists`; each counts as an attempt, so after three the feeder will file a spurious adjudicate row for a Bug that never ran (seen in the 20:28 and 20:38 ticks).
- 2026-09-21 20:49 ingest: state New → Active (PR #10 OPEN; branch fix/B-0025)
- 2026-09-22 09:03 ingest: state Active → Closed (commit 7c8c756 names B-0025; CI green on main at or after it)
- 2026-09-22 11:30 closed by F-0087 step 1 (7c8c756): `lifecycle.may_launch` refuses only a live run's worktree; an ended run's worktree and branch are taken over; reap on landed or empty — `tests/test_lifecycle.py::LaunchAndReapInvariants`.
