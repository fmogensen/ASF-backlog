---
id: B-0136
type: bug
title: An install can leave a product's tick clock unloaded; status just omits it
parent: E-0001
decided: true
severity: S2
found_in: dev
signature: An install can leave a product's tick clock unloaded; status just omits it
# ---- machine ----
schema_version: 1
state: Active
stage_since: 2026-09-26T01:09:54Z
evidence:
  - branch fix/B-0136
  - "rule: fixer"
updated: 2026-09-26T07:37:19Z
cost: {sessions: 10, fix_rounds: 1, runner_min: 0, usd: 11.93, tokens_input: 680, tokens_output: 185456, tokens_cache_read: 25961228, tokens_cache_write: 526317}
---
## Description
An install can leave a product's tick clock unloaded. On 2026-09-25 at ~16:35, after an install of 205123f, `launchctl list` showed asf.asf.daily loaded but asf.asf.record-health-wave-prs-harvest missing, though its plist was still in ~/Library/LaunchAgents. The asf product silently stopped ticking. `asf status` showed it only as a missing name in the Cron cell, and `asf doctor` was not run by anyone. `asf scheduler install --product asf` restored it. Want: tools/install.sh verifies after its scheduler step that every clock the product declares is loaded, retries the bootstrap once, and fails the install loudly naming the missing label if it still isn't. The tick of any other product (or `asf status`) flags "clock <label> not loaded" as RED in the Cron row, instead of omitting it. Tests: a declared clock with a plist but no loaded job is reported not-loaded by status and doctor; install retries the bootstrap.

## Acceptance
- [ ] 

## Non-goals

## History
- 2026-09-25: created (inbox) — shape: signature → bug
- 2026-09-25 groom: decided → true (adjudicator, groom-2026-09-25)
- 2026-09-26 01:09 ingest: state New → Active (rule: fixer; branch fix/B-0136)
- 2026-09-26 04:57 adjudicate (adjudicate-b-0136): Held three times over "conflict in tests/test_install.py," but no conflict exists on the checkout handed to me: `git merge-base HEAD origin/main` equals origin/main's own tip (5cab3a234), the diff applies clean, and the full `tests.test_install` module runs 110/110 green including the four tests the commit message names for this fix. Round 1's review (`docs/reviews/1-b-0136.md`) already returned `verdict: approved` with zero Concerns — only three Insights, none of them a defect: I1 notes the card declared no `writes:` footprint (true, and corrected below, not a code fix); I2 is two pre-existing trunk failures (`test_watch`, `test_dry_run`) the review itself reproduced on a clean `origin/main` scratch worktree with none of this branch's code present — unrelated to B-0136, not a reason to hold it; I3 flags a small duplicated `missing`-clocks computation between `asf/doctor.py` and `asf/views/status.py`, which the review itself calls "fine as delivered" pending a third caller. Overruled: the conflict hold itself (no conflict is reproducible against the current origin/main; whatever combined-lane state produced it was transient and is not present in this checkout — this finding does not return). Overruled: I2 as a blocking concern (pre-existing, unrelated, already isolated by round 1's own repro). Overruled: I3 as a blocking concern (the review's own verdict already treats it as acceptable at two call sites). Upheld: I1 — the corrected footprint is named in this ruling's `writes:` field below. Nothing in this branch touches licence, money, security, or customer-visible surface. The work is complete, reviewed, tested, and already on origin at its current head; it is ready to land as-is.
- 2026-09-26 05:35 adjudicate (adjudicate-b-0136): The hold was a purely mechanical merge conflict in tests/test_install.py, not a substantive review disagreement — no review file, spec, or plan for B-0136 exists in the record, and the branch's own review (087d09323, carried forward) already reads "approved." The conflict arose because `main` picked up B-0131's console-permissions step (adding a `console-permissions` call and shrinking the expected `scheduler` call count in `test_a_failed_hooks_step_still_runs_the_scheduler_and_the_doctor` / `test_every_step_green_exits_zero`) after B-0136's branch tip was cut, while B-0136 independently doubled the `scheduler` call count (install + status read-back, per its own verify-and-retry design). Both changes are real and additive, not competing: I merged the two `calls` expectations (`['--version', 'hooks', 'scheduler', 'scheduler', 'doctor', 'console-permissions']` for the two-scheduler-call happy/hooks-fail paths, and the same suffixed with `'console-permissions'` for the two new retry tests), kept all four tests from both sides (`test_step_6_offers_the_console_permissions_and_writes_nothing` from main, `test_an_unloaded_clock_is_retried_once_and_then_succeeds` / `test_an_unloaded_clock_still_missing_after_retry_fails_loudly_naming_it` from the branch), completed the rebase, and reran the targeted suite green. Nothing about B-0136's spec-level content (doctor.py, views/status.py, install.sh's retry logic) was in dispute or touched beyond the conflict resolution. This closes the loop: the hold was a stale rebase, not a design question, and there is nothing left for a future round to re-litigate.
- 2026-09-26 05:50 adjudicate (adjudicate-b-0136): The hold was not a substantive disagreement — the round-1 review (docs/reviews/1-b-0136.md) already reads verdict: approved with zero C findings; the stalemate was a mechanical rebase conflict in tests/test_install.py, hit three times because origin/fix/B-0136's tip predates main's already-landed B-0131 (console-permissions offered as install.sh's new step 6), so replaying B-0136's own two commits collided with B-0131's renumbered step and added test. I resolved it by hand: kept B-0136's clock-retry call sequence (the extra 'scheduler' status-check calls in step 4) and appended B-0131's 'console-permissions' call as the now-step-6 tail on every affected assertion, fixed the stray "step 5 still ran" comment to "step 6" to match the renumbering, verified all 93 tests in tests/test_install.py green, and continued+fast-forward-pushed the rebase (087d09323..3683d5e48, no force, no merge). Nothing in the review's own approved verdict changes. Separately, I closed the review's I1 (no writes: footprint recorded, same recurring gap as B-0129's round 1): the corrected footprint is the six files the review itself already isolated as this item's actual diff against main — asf/doctor.py, asf/views/status.py, tools/install.sh, tests/test_doctor.py, tests/test_install.py, tests/test_views.py — recorded below so the next session does not re-derive it. C findings: none open (round 1 approved with none). I findings: I1 addressed via the writes: line below (closed, not reopened); I2 (pre-existing red on test_watch/test_dry_run, verified unrelated to this diff) and I3 (a small missing/loaded duplication between doctor.py and views/status.py, flagged for a third-caller threshold) stand as informational, not blocking — overruled as holds: neither names a defect in this diff, so neither returns in a later round.
- 2026-09-26 07:03 adjudicate (adjudicate-b-0136): The hold was a rebase conflict in tests/test_install.py against origin/fix/B-0136, held three times with no round left to correct it. I read docs/reviews/1-b-0136.md: round 1 is already verdict "approved", with zero C findings and three I (informational, non-blocking) notes that the review itself says stand as recorded. Investigation of the checkout showed the substance was never in dispute: asf/doctor.py, asf/views/status.py and tools/install.sh's scheduler_install_verified() on this branch are byte-identical to origin/fix/B-0136 (which is itself origin/main's tip, 3683d5e48) — B-0136's reviewed fix had already landed. The conflicting commit (the branch's own fix(B-0136) commit, f65456d9c) was cherry-picking a stale duplicate of a test already present upstream; every hunk but one was a pure line-wrap difference, and the rebase correctly dropped it as empty. The one real difference was a stale comment — `# step 5 still ran` where tools/install.sh's console-permissions step is actually step 6 (verified against tools/install.sh:99, the only place `/plugin install asf@asf` prints) — which I fixed and committed as cd19f0762. The three I notes in the review (no declared writes: footprint, two pre-existing red tests on trunk unrelated to this diff, and a small duplication between doctor.py's and status.py's `missing` computation) are upheld as informational only, per the review's own recording, and do not block: none is a C, so none required an edit here. This closes the loop — B-0136 is fully landed, its branch is rebased clean onto origin/fix/B-0136, and pushed.
- 2026-09-26 07:51 adjudicate (adjudicate-b-0136): Disputed: whether B-0136 could land at all — three prior sessions held on the same rebase conflict in tests/test_install.py against origin/fix/B-0136, despite round 1 (docs/reviews/1-b-0136.md) already verdict "approved" with zero blocking (C) findings. I checked the conflict itself rather than trusting either side's account: this branch's own commit (formerly 456db94, replayed here as d3004b02e before I dropped it) has a byte-identical patch-id (76173d3a...) to 1ec6df4c8, which is already an ancestor of origin/fix/B-0136 and was itself reviewed and approved there (087d09323) — origin/fix/B-0136 (cd19f0762) already carries the complete fix (tools/install.sh's scheduler_install_verified retry-and-name-loudly logic, asf/doctor.py's and asf/views/status.py's missing-clock RED rows), folded together with B-0131's console-permissions split. Resolving the tests/test_install.py conflict by keeping origin's already-integrated content (rather than the stale pre-B-0136 hunks from the replayed B-0131 commit) left the branch's own B-0136 commit with a zero-line diff against its parent; I dropped that now-empty commit (git reset --soft, never pushed, so no force needed) and fast-forward-pushed the remaining locally-queued commits (B-0131 replay, T-0185 wip/review, two trunk fixes, a CHANGELOG bump — none of them B-0136's) onto origin/fix/B-0136, cd19f0762..9a868add3. What now holds: B-0136 is done — its fix has been on trunk since 1ec6df4c8/087d09323, and the three-round stalemate was three sessions each re-attempting to land a duplicate of already-shipped work rather than checking the target branch first; no round comes back for this. What changes: the card's undeclared `writes:` footprint (I1 in the review) is corrected below to the diff's real, already-landed footprint. Overruled: none of I2 (two pre-existing, unrelated trunk gate flakes) or I3 (near-duplicate `missing`-clock computation between doctor.py and status.py, explicitly "fine as delivered" per the reviewer) call for any edit — both are informational only, addressed to whoever next touches those modules, not to this item.

## Children

## Backlinks
- [B-0135](../bugs/B-0135.md) An install during a running tick tears it: groom ImportError from a half-swapped package
