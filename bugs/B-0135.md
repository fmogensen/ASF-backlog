---
id: B-0135
type: bug
title: "An install during a running tick tears it: groom ImportError from a half-swapped package"
parent: E-0001
decided: true
severity: S2
found_in: dev
signature: "An install during a running tick tears it: groom ImportError from a half-swapped package"
# ---- machine ----
schema_version: 1
state: Active
stage_since: 2026-09-26T00:51:59Z
evidence:
  - branch fix/B-0135
  - "rule: fixer"
updated: 2026-09-26T03:09:03Z
cost: {sessions: 4, fix_rounds: 1, runner_min: 0, usd: 4.48, tokens_input: 362, tokens_output: 94474, tokens_cache_read: 11927199, tokens_cache_write: 286844}
---
## Description
An install that runs while a tick is running tears that tick: on 2026-09-25 at ~14:08 botseon's groom step failed with "ImportError: cannot import name 'as_list' from 'asf.record.core'" because `pipx install --force` replaced the package under the running tick (half the modules old, half new). The next tick was fine. Want: tools/install.sh takes each product's tick lock (or waits for running ticks to finish, bounded) before replacing the package, and the tick imports every step module up front at start, so a swap mid-tick cannot mix versions; the upgrading guide already warns about this, the installer should enforce it. Tests: install waits on a held tick lock.

## Acceptance
- [ ] 

## Non-goals

## History
- 2026-09-25: created (inbox) — shape: signature → bug
- 2026-09-25 groom: decided → true (adjudicator, groom-2026-09-25)
- 2026-09-25 groom: severity → S2 (adjudicator, groom-2026-09-25)
- 2026-09-26 00:51 ingest: state New → Active (rule: fixer; branch fix/B-0135)
- 2026-09-26 04:57 adjudicate (adjudicate-b-0135): Disputed was whether B-0135 is still open. It is not, on content: round 1's C1 (lock checked-then-released before the pipx swap) and C2 (steps still imported lazily mid-tick) were both fixed by edadae58e and round 2 approved them with zero new C's; I re-verified both myself against the checkout rather than trusting the review — tools/install.sh:47-75 opens tick.lock, loops LOCK_EX|LOCK_NB with a bounded deadline, then holds it through the `pipx install --force` call itself inside a `finally`-guarded block, and asf/tick/tick.py:416-418, inside `_run_steps`, resolves every asf-owned step's module for `rows` before running any step, under the same lock `cmd_tick` already took at line 359. `tests/test_install.py`'s 87 tests are green, including all three lock/import regression tests. What actually holds this card at STALEMATE is not a review finding but a landing collision: `fix/B-0136`, an unrelated sibling branch off the same `origin/main` base (5cab3a234), inserts its own new tests into the identical spot in `InstallScriptTest`, so harvest's combined-head test hits `CONFLICT (content): Merge conflict in tests/test_install.py` on every attempt — I reproduced this myself in a scratch worktree merge (`tools/install.sh` itself auto-merges clean; only the test-insertion point collides, a textual proximity collision, not a semantic contradiction between the two fixes' behavior). B-0135's own diff has no defect and needs no further edit. Ruling: B-0135 is unblocked and lands as-is, unchanged — it is the older, S2, twice-reviewed-and-approved fix and takes the landing slot; the merely-textual test-insertion collision is the younger, lower-severity B-0136's to resolve by rebasing onto B-0135's landed state once it lands, not a reason to hold this card further. This does not come back in a later round.

## Children

## Backlinks
