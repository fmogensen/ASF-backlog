---
id: B-0114
type: bug
title: "native landing: a merged spec/plan PR marks its Feature landed/Resolved, so no coder ever launches"
parent: E-0002
decided: true
severity: S1
found_in: dev
signature: a merged plan PR leaves the Feature at stage=landed state=Resolved, so its minted Tasks stay New and no coder ever launches
# ---- machine ----
schema_version: 1
state: Active
stage_since: 2026-09-24T18:55:53Z
evidence:
  - branch fix/B-0114
  - "rule: fixer"
updated: 2026-09-25T12:56:38Z
cost: {sessions: 10, fix_rounds: 1, runner_min: 0, usd: 30.23, tokens_input: 1036, tokens_output: 285489, tokens_cache_read: 31658854, tokens_cache_write: 726295}
---
## Description
With native PR landing in pull-request mode, harvest squash-merges a spec/plan PR. On the next record
step the Feature reads stage=landed, state=Resolved — as if its code had shipped. Seen on 8 Features
at once (6 plans, 2 specs). Squash subjects on the trunk looked like `docs(plan): F-0047 — …`,
`docs(spec): F-0129 — …`, `plan(OPS-1): the F-0037 plan …`, `plan(F-0019): …`.

Effect: the feeder launches PLAN → CODE only at plan-approved/building, so a Feature whose plan just
landed never gets a coder — its minted Tasks sit New forever.

Expected: a merged lane PR of kind spec/plan (branch spec-/plan- prefix, or only specs_dir/plans_dir/
reviews_dir paths) makes the Feature spec-approved / plan-approved, never landed/Resolved — whatever
the squash subject says. Native landing should also write the squash subject in the doc-lane form
(`plan(<ITEM>): …`) so DOC_LANE_SUBJECT recognises it.
Acceptance: an ingest test where a plan PR squash-merges with a non-conforming subject leaves the
Feature at plan-approved with its Tasks New and launchable.

## Acceptance
- [ ] 

## Non-goals

## History
- 2026-09-24: created (inbox) — shape: signature → bug
- 2026-09-24 groom: decided → true (adjudicator, groom-2026-09-24)
- 2026-09-24 groom: severity → S1 (adjudicator, groom-2026-09-24)
- 2026-09-24 18:55 ingest: state New → Active (rule: fixer; branch fix/B-0114)
- 2026-09-24 23:12 adjudicate (adjudicate-b-0114): What was disputed was round 2's C1 — that round 1's hermeticity fix was applied to one of the suite's two guards only. C1 is upheld: I reproduced it at head 89ec987, where `python3 -m unittest tests.test_install` gave FAILED (failures=12), each diff `- <operator home>/.ASF/state/asf/githooks / + .githooks`, because `tests/test_00_home.py` guards `discover -s tests` while every other entry point imports `tests/__init__.py`, which had no strip. Fixing it surfaced the same miss a second time, which neither round saw: `tests/__init__.py` never dropped CALLER_IDENTITY either, so this session's own ASF_PRODUCT reached a bare `python3 -m unittest tests.test_doc_lane_landing` and killed 7 of its 12 tests in env.load_product — B-0043, B-0055 and B-0114 were each fixed in one twin and not the other. What now holds is that both guards do the same three things and `tests/test_hermetic.py` proves each entry point from a child carrying all three leaks, so the next edit to one twin fails in CI instead of costing a review round; I negative-proved that test by running the same child unguarded, where all three leaks survive. Also upheld and fixed: round 2's I1 (the 138-column line, rewrapped), I4/I6/I8 (the docstring now names the two config channels strip_git_config does not cover, that subsections compare case-sensitively, and that a base which carried a count keeps one), I3 (answered with the cross-entry-point test rather than an import-order refactor, which would move code that runs before asf.env is loadable); round 1's I5 (the doc-lane kinds are now one DOC_LANE_KINDS constant read by both halves of the fix instead of a tuple spelled twice) and I3 (a gh that does not know --subject now retries once without the flag and lands, where before it fell through to `return None, tail(err)` and stalled the lane silently — tested). Overruled, and none of these returns in any later round: round 1's I4, that the suite's red was the machine's — it was the caller's GIT_CONFIG_*, and that misreading is what produced this stalemate; round 1's I2, that the newest rather than newest-conforming subject is taken — the prefixed result already matches DOC_LANE_SUBJECT (asf/harvest/lane.py:1309-1311 checked), so the contract holds and only a prettier subject is lost, now named in the docstring; round 2's I7, that GIT_CONFIG_VAR_RE is a third copy — asf/redact.py:86 ENV_NAME_EXEMPT_RE is an exemption list for secret redaction while hermetic's enumerates variables to pop, two different questions about one stable git interface, and sharing a constant would couple redaction to environment building for no gain; round 1's I1 and the merge-queue gap, which is real but is the queue's subject to compose, now named in the docstring with docs_only-by-paths as the guard there. Round 2's I2 (that the hermetic fix is a second card's work) is not a code change and is not overruled — the code is right and stays; the carve-out is a record action, and it is the NEEDS OPERATOR line above, because a session never mints an id. What changes: the card's footprint was declared empty and was not, so the corrected writes: line below is the ruling's mechanism, and B-0114's acceptance now holds on every entry point rather than only under tools/run_tests.py.
- 2026-09-25 10:35 adjudicate (adjudicate-b-0114): What was disputed was one blocking finding: round 3's C1, that `tests/test_hermetic.py`'s `test_both_of_the_suites_entry_points_are_hermetic` pinned one hermetic guard and not both. It is upheld, because I reproduced it rather than reasoning about it: with `hermetic.strip_git_config(os.environ)` deleted from `tests/test_00_home.py:31`, the test was `OK` while the mode that twin exists for — `python3 -m unittest discover -s tests -p test_00_home.py`, which loads the module top-level and never runs `tests/__init__.py` — was `FAILED (failures=1)` on `'core.hookspath' unexpectedly found`. A test whose comment claims "neither twin can be left behind again" while staying green with a twin broken is worse than no test, so the exact edit was a third child run of that discover mode in the same loop; it is committed at `02bc263` and the test now goes red when *either* guard is removed (verified both ways). Round 3's I1 was non-blocking but factually true on inspection — `GhHost` appears nowhere in the tree and `squash_subject` runs before `host.merge`, not after — so it is closed too at `4c6d162`, docstring only. Everything else is overruled and does not return in any later round. Round 2's I2 (the hermeticity fix belongs on its own card) is overruled as a blocker: the code is correct and this branch's gate cannot run without it, so the carve-out is a record action, not a branch change, and it is a NEEDS OPERATOR line above. Round 3's I4 and I8 (`tests/test_dry_run.py:76` asserting byte-equality over `asf/harvest/harvest.py:746`'s `{time.monotonic() - started:.0f}`, and `tools/run_tests.py` silently returning a smaller denominator under load) are overruled as against this branch — I confirmed the branch touches none of those three paths, and I saw the same flake twice and then a clean `OK` on the identical head. Round 3's I5 is an environment defect, not a branch defect. Round 3's I2, I3, I6 and I7 are overruled as grounds for a further round: I verified I7's anchors myself (`("spec", "plan")` does remain literal at `asf/evidence/evidence.py:502` and `:513`, and at `asf/harvest/lane.py:264` and `:619`), and they are a refactor of rows the card's *Expected* never names, on a branch already three rounds deep. What now holds is the card's contract, checked against the checkout and not against a review: both halves of *Expected* are implemented, and the *Acceptance* clause is `tests/test_doc_lane_landing.py:245` clause for clause — `("Active", "plan-approved")` at `:251`, `["New", "New"]` at `:253`, `rows.PLAN_CODE` per minted Task at `:259`. What changes because of it is that the review loop is closed at round 3 and the branch is landable, and that the card's empty footprint — the thing that let an unrelated hermeticity defect land here with nobody able to call it out of scope — is corrected in the `writes:` line below.
- 2026-09-25 12:45 adjudicate (adjudicate-b-0114): What was disputed was whether fix/B-0114 is the cause of the three gate holds that stalled it, after four review rounds ended in approval — and, secondarily, six findings round 4 left open. The gate is the fact, and it is green: at the head before my edits the full suite ran 2808 tests across 92 modules OK, and the hermetic env -i step returned the same denominator and rc=0, so no red attributable to this branch exists to hold it. The reds that produced the holds are in modules the branch never touches — `git diff origin/main...HEAD` over `tests/test_dry_run.py`, `asf/harvest/harvest.py`, `tools/run_tests.py` and `asf/tick` is empty — and both are trunk defects: `asf/harvest/harvest.py:797` formats `{time.monotonic() - started:.0f}` into a line `tests/test_dry_run.py:76` requires byte-identical across two consecutive dry runs, which rounds to different seconds under load, and `tools/run_tests.py` reports the survivors when a pooled module process dies, giving a smaller denominator with no module named. Round 3's own origin/main baseline had test_dry_run red at the trunk. The three gate holds are therefore overruled as not this branch's, and they do not return; the branch lands as it stands. Of round 4's open findings I upheld I1, I2 and I4 and made the edits in 093180a: the --subject retry now logs the degrade instead of leaving a non-conforming trunk subject to be inferred, strips the flag by position rather than by value, and no longer fires on --rebase, which never adds it; the twin test spawns sys.executable at both sites so the hermeticity guard tests the interpreter actually running the suite. I overruled I3 — the `flag provided but not defined` alternative in SUBJECT_UNKNOWN_RE is unreachable but inert, because `_rejects_subject` (asf/harvest/lane.py:1304) also requires the literal `--subject` that Go's single-dashed form never prints, and the reviewer's proposed relaxation to `'subject' in err` would widen a retry that drops the subject, which is the precise failure this card exists to prevent. I overruled I6 — DOC_LANE_KINDS already binds the two sides this card created, the writer `squash_subject` and the reader `lane_kind`, which is what round 1's I5 asked for; the remaining literal `('spec', 'plan')` sites are a pre-existing refactor across four modules B-0114 never entered, and pulling them into a branch with no review round after it buys nothing this card needs. What changes: the card's footprint is recorded below, since `writes: (none declared)` is why scope could never be argued in any round; the two trunk flakes need their own items, and the gate's own invocations need an operator allowlist entry, both below.

## Children

## Backlinks
