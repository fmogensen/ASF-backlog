---
id: F-0087
type: feature
title: "Failure-class invariants: one property test per class of the first 53 Bugs, and the whole-loop dogfood tick in CI"
parent: E-0001
decided: true
rank: 3
# ---- machine ----
state: Resolved
stage: landed
stage_since: 2026-09-22T09:03:52Z
cost: {sessions: 2, fix_rounds: 0, runner_min: 0, usd: 11.22}
evidence:
  - commit 0f98f2c names F-0087
  - CI green on main at or after it
updated: 2026-09-22T14:04:59Z
---
## Description
Operator, 2026-09-22 10:30: "What are we doing to reduce errors like these?" — 53 Bugs in 36 hours, each
fixed with a failing-first test; the same instance cannot recur, the next kind still can. The classes:

| Class | Bugs | Invariant test |
| --- | --- | --- |
| registry semantics | B-0028, B-0041, B-0051 | every launch line opens a run; a run's terminal fields never fold into the next; a `finished` run has a pushed branch — property test over generated registries |
| environment leaking into gates and tests | B-0033, B-0038, B-0043, B-0047 | the suite and the harvest gate are hermetic: run under `env -i` + the operator's own `ASF_HOME`/`ASF_PRODUCT` + a foreign `init.defaultBranch` in CI's matrix |
| path resolution | B-0036, B-0042, B-0050 | every command runs from `/tmp` with `--product`, creates nothing under `/tmp`, and prints `record:`/`repo:` on its first line — the audit's matrix as a test |
| branch lifecycle | B-0025, B-0046, B-0048, B-0049 | a state machine: launched → pushed → held(n) → corrected → landed → reaped; every transition has a test, no state is terminal except reaped/adjudicated |
| worker behaviour | B-0024, B-0052 | every brief ends with the closing paragraph; a result with unpushed work is failed at the source |
| push races | B-0030, B-0044 | origin moves between fetch and push in every record test that pushes |

Second part — F-0033 made whole: the sample product's tick in CI walks the failure paths too (held → correct
→ land; unpushed → correct; origin moved → rebase) on the fake runtime, asserting the tick's tables.

Measured: Bugs per landing and rounds per landing on the scorecard, per week.

## Acceptance
- [ ] 

## Non-goals

## History
- 2026-09-22: created (inbox)
- 2026-09-22 08:37 ingest: stage card → card (no evidence found (2026-09-22))
- 2026-09-22 09:03 ingest: state New → Resolved (commit 7c8c756 names F-0087; CI green on main at or after it)
- 2026-09-22 09:03 ingest: stage card → landed (commit 7c8c756 names F-0087; CI green on main at or after it)
- 2026-09-22 11:30 F-0087 pass (fable/core, session_01DiYPzouetz7pdU6bdq4hU1): landed on main — 7c8c756 the lane as one state machine (`asf/workers/lifecycle.py`, `tests/test_lifecycle.py`, `docs/design/lane-state-machine.md`), a571a26 one hermetic environment (`asf/hermetic.py`, `tests/test_hermetic.py`, the `env -i` CI step), 8c25fa7 every record command through the one resolver (`tests/test_path_resolution.py`, the B-0050 audit as a test). Push races, the REPORT's `pushed:` line and the whole-loop dogfood follow in the same pass.
- 2026-09-22 11:20Z F-0087 pass, second half landed: 6de95c9 the record push keeps rebasing while origin moves (B-0030, B-0044), 18f11dd the REPORT's `pushed:` line read back at the source (B-0024, B-0052), 0f98f2c the whole-loop dogfood in `tests/test_sample_product.py` (held → correct → landed with the trunk moving; done-without-push → correct in the same worktree → landed → reaped), which found and fixed four more defects (feeder relaunching a pushed branch, correction on the wrong branch, empty reap leaving its branch, a shared evidence cache). Rulings in `docs/design/lane-state-machine.md`.

## Children

## Backlinks
- [B-0016](../bugs/B-0016.md) Parallel spawns race on .git/config when creating worktrees
- [B-0024](../bugs/B-0024.md) An unmapped model label launches a session that dies at once, and the result reads as success
- [B-0025](../bugs/B-0025.md) A session that ended with nothing committed leaves a worktree that blocks every relaunch
- [B-0028](../bugs/B-0028.md) A corrected or late-finishing session stays 'dead pid'; harvest never lands its branch
- [B-0030](../bugs/B-0030.md) A record push refused because origin moved is discarded; the tick's appended events are lost
- [B-0033](../bugs/B-0033.md) The harvest gate inherits the tick's ASF_PRODUCT; ASF's own suite reads the live product and goes red
- [B-0036](../bugs/B-0036.md) A landing never reaches the checkout the scheduler runs; the factory keeps running the code from before its own fix
- [B-0038](../bugs/B-0038.md) CI main red: harvest fixture depends on the host's init.defaultBranch
- [B-0041](../bugs/B-0041.md) A relaunched job inherits the previous run's ended/failed fields; health skips it, the feeder re-emits it, harvest never lands it
- [B-0042](../bugs/B-0042.md) The scheduler's checkout is fast-forwarded only after a lane landing; a fix pushed any other way leaves the factory on old code
- [B-0043](../bugs/B-0043.md) The test suite reads the operator's live ~/.ASF; the harvest gate goes red on any operator config change
- [B-0044](../bugs/B-0044.md) A record rebase conflicts on machine-owned content when a hand commit touched the same card; the tick's push is still refused
- [B-0046](../bugs/B-0046.md) A correction row cannot spawn: spawn creates a new branch, but the held branch already exists
- [B-0047](../bugs/B-0047.md) asf plugin check resolves the plugin directory from the package, not the checkout; CI is red on a plain install
- [B-0048](../bugs/B-0048.md) An adjudicate row cannot spawn on a held branch, and rounds climb past the cap — held branches loop forever
- [B-0049](../bugs/B-0049.md) Health never reaps a landed branch's worktree: harvest rebased the tip and deleted the remote branch, so 'pushed' fails forever
- [B-0050](../bugs/B-0050.md) Record commands use the cwd as the record: /asf:groom from a product repo grooms nothing and writes groom/ into the product repo
- [B-0051](../bugs/B-0051.md) A session that ends 'finished' without pushing blocks its item forever: health says finished, harvest sees nothing, spawn refuses the worktree
- [B-0052](../bugs/B-0052.md) Sessions background the test suite and return before it ends — no commit, no push; the brief must forbid it
- [B-0059](../bugs/B-0059.md) A spec landing commit resolves the Feature it names, and a spec on main reads spec-draft: the evidence does not know the lane's own conventions
- [B-0061](../bugs/B-0061.md) A branch whose run ended other than finished is never landed by content nor archived: harvest reads the run's verdict before the content
