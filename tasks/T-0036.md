---
id: T-0036
type: task
title: The nine fields, `forbidden_patterns()`, and `python3 -m asf.conventions`
parent: F-0074
decided: true
links: {plan: docs/plans/f-0074.md}
writes: [asf/conventions.py, tests/test_conventions.py]
# ---- machine ----
state: Closed
stage_since: 2026-09-22T19:04:13Z
cost: {sessions: 2, fix_rounds: 0, runner_min: 0, usd: 3.91}
evidence:
  - commit 03efa74 names T-0036
  - CI green on main at or after it
  - "rule: landed-green"
updated: 2026-09-24T07:24:43Z
---
## Description
stories: S-8000, S-8002
writes: asf/conventions.py, tests/test_conventions.py

**Files**
- `asf/conventions.py` — nine `DEFAULT_*` constants, nine dataclass fields, `forbidden_patterns()`,
  a `__main__` block.
- `tests/test_conventions.py` — `DefaultsTests.test_the_new_fields_default_to_none`, a new
  `ForbiddenPatternsTests`.

**Steps**
1. Nine module constants, each with the spec's documented comment, all `None`:
   `DEFAULT_BRIEFS_DIR`, `DEFAULT_MATRIX_PATH`, `DEFAULT_DESIGN_SPEC_NAME`,
   `DEFAULT_DECISIONS_FILE`, `DEFAULT_REPORTS_DIR`, `DEFAULT_REPORT_PATTERN`,
   `DEFAULT_CI_WORKFLOW`, `DEFAULT_CI_DEV_JOB`, `DEFAULT_DEPLOY_WORKFLOW`.
2. The matching dataclass fields, beside the existing ones and before `stage_limits`/`extra`.
   `field_names()` and `as_dict()` pick them up with no change; `from_mapping` already drops a
   `None` value, so a yaml key set to nothing keeps the default.
3. `forbidden_patterns()` (D4, D5, PD9): walk the string values of `DEFAULT_BRANCH_PREFIXES`
   (skipping `legacy`, a list) and every module-level `DEFAULT_*` string; keep the ones containing
   `/`, `{` or `#`; return `"['\"]" + re.escape(value) + (r'\b' if value[-1].isalnum() or
   value[-1] == '_' else '')`. Stable order. `import re` at module top.
4. A `__main__` block at the bottom: `--forbidden` prints the patterns one per line; any other
   argv prints `name = value` for each `DEFAULT_*`; exit 0 either way.
5. The module docstring's `conventions:` example gains one of the new keys so the block a reader
   copies is current. **Do not** touch the "second and last exception" sentence — that is Task 7
   (PD8), which owns the three-adapter wording in all three files.
6. Tests: `DefaultsTests.test_the_new_fields_default_to_none` (all nine `None` on `Conventions()`,
   all nine in `field_names()`); `ForbiddenPatternsTests` with
   `test_one_pattern_per_path_shaped_default` (nine entries; contains `['"]worker/`,
   `['"]docs/specs\b`, `['"]\{reviews_dir\}/\{n\}-\{slug\}\.md`, `['"]### Task\b`; no pattern
   matches the bare words `main` or `inbox`), `test_a_copied_default_matches_and_prose_does_not`
   (`re.search` hits `X = 'docs/specs'`, `default="fix/"`, `f'worker/{iid}'`; misses the unquoted
   prose `the worker/ prefix` — PD3), and `test_the_module_prints_them` (`python3 -m
   asf.conventions --forbidden` → exit 0, the same nine lines).

**Gate**
```bash
python3 -m unittest -v tests.test_conventions
python3 -m unittest discover -s tests
bash tools/check_generic.sh && bash tools/check_conventions.sh && asf plugin check
```

**Acceptance**
```sh
python3 -m unittest tests.test_conventions -v
```
```bash
python3 -m unittest tests.test_conventions.DefaultsTests tests.test_env -v
```
```bash
python3 -m unittest tests.test_conventions.ForbiddenPatternsTests tests.test_conventions.CheckConventionsScriptTests.test_a_copied_default_fails_the_build -v
```

---

## Acceptance
- [ ] 

## Non-goals

## History
- 2026-09-22: created (plan F-0074)
- 2026-09-22 16:53 ingest: state New → Active (branch task/T-0036)
- 2026-09-22 18:52 adjudicate (adjudicate-t-0036): What was disputed was never a review finding — no `docs/reviews/1-t-0036.md` exists on the branch, on `origin/main`, or anywhere in history (`docs/reviews/` holds only `1-b-0005-ruling.md`), and no round-1 review was ever written. The hold's own reason is `conflict`, and the conflict is literal: the worktree sat mid-rebase with `asf/conventions.py` unmerged in three hunks, and it came back three times because both sides are correct and additive, so every "correction" round re-derived the same standoff instead of resolving it. HEAD carries the harvest block (`DEFAULT_HARVEST_GATE`, `DEFAULT_BRANCHES_PER_TICK`, `DEFAULT_GATE_TIMEOUT_S`, `HARVEST_KEYS` and the three dataclass fields, from B-0040/B-0031/B-0072); `a0efb42` carries T-0036's nine `DEFAULT_*` constants and nine fields. Neither supersedes the other. What now holds: both sides survive in full — in the docstring example `briefs_dir: docs/briefs` precedes the `harvest:` block (a top-level key before a nested one, so the yaml stays readable); in the constants the harvest group keeps `HARVEST_KEYS` adjacent to its own three, with the nine `None` defaults following it; in the dataclass the three harvest fields precede the nine, all before `stage_limits`/`extra`. I checked the count that the two sides could have broken between them: `forbidden_patterns()` still returns exactly nine patterns, because the nine new defaults are `None` (not `str`) and `DEFAULT_HARVEST_GATE = 'combined'` holds no `/`, `{` or `#` — `python3 -m asf.conventions --forbidden` prints the nine, and `ForbiddenPatternsTests.test_one_pattern_per_path_shaped_default` asserts it. Two findings I overrule, and neither returns in a later round: first, the card names `tests.test_conventions.CheckConventionsScriptTests.test_a_copied_default_fails_the_build`, which has never existed under that name in any branch (`git log -S --all` over the file returns nothing) — the class exists and its four tests, including `test_the_whole_package_is_clean`, cover the behaviour and pass, so the card's test name is a naming slip, not a missing test; second, `docs/plans/f-0074.md` contains the string `T-0036` zero times — it names its work "Task 1" at line 66, which is this card, so the plan is not silent on it and no plan edit is owed. What changes: the rebase is finished, the tree is clean, the single signed-off commit `9729a62` sits one ahead of `origin/main` with the full suite green, and `asf/conventions.py:6` still reads "the second and last exception" untouched, as PD8 reserves that wording for Task 7. The push was refused as non-fast-forward, which is the rebase and nothing else; per B-0056 I did not merge, force or recut — the factory publishes the rebased lane branch. The loop ends here.
- 2026-09-22 19:04 ingest: state Active → Closed (commit 03efa74 names T-0036; CI green on main at or after it)

## Children

## Backlinks
