---
id: T-0019
type: task
title: The operator's side — `asf approvals`, `list`, `resolve`, and the doctor row
parent: F-0031
decided: true
links: {plan: docs/plans/f-0031.md}
writes: [asf/approvals.py, asf/cli.py, asf/doctor.py, tests/test_approvals.py]
after: [T-0018]
# ---- machine ----
state: Closed
stage_since: 2026-09-23T01:02:37Z
cost: {sessions: 3, fix_rounds: 0, runner_min: 0, usd: 4.09}
evidence:
  - commit 6998049 names T-0019
  - CI green on main at or after it
  - "rule: landed-green"
updated: 2026-09-24T07:24:43Z
---
## Description
stories: S-7455
writes: asf/approvals.py, asf/cli.py, asf/doctor.py, tests/test_approvals.py

**Files**
- `asf/approvals.py` — `register(subparsers)`, `cmd_approvals`, `check_doctor(cfg, product)`.
- `asf/cli.py` — one `register` call beside the others.
- `asf/doctor.py` — the `approvals` row in `run`.
- `tests/test_approvals.py` — `CliTest`, `DoctorTest`.

**Steps**
1. PD2: `git show origin/main:asf/hooks.py | grep -q "def account_settings_path"`; else the
   NEEDS OPERATOR line and stop.
2. `register`: `asf approvals [list | resolve <hold> <granted|done|dropped>] [--product p]`
   (`set_defaults(run=cmd_approvals)`, the product from `--product`, else `$ASF_PRODUCT`, else
   `default_product`). No argument: the §2.5 table — nine rows,
   `class | level | from | read by | recognisers` (built-ins named in words, signals listed).
   `list`: `hold | level | first | last | count | detail` over `open_holds`. `resolve`: exit 2
   and one line when the hold is not open, else `resolve(...)` and one confirming line.
3. `check_doctor(cfg, product)` → `(ok, detail)`: `ok=False` on `matrix` raising; otherwise
   `ok=True` with a detail naming every unmapped class, every `groom`/`human-now` class with no
   recogniser (built-in or signal), `merge_amendable_set` mapped while `amendable_paths` is empty,
   and every worker account whose `hooks.account_settings_path` lacks the approvals entry.
   `asf/doctor.py` `run` appends `('approvals', True, ok, detail)` after `one-factory`.
4. `CliTest` and `DoctorTest` per §3 A5, on a temp `ASF_HOME`.

**Gate**
```bash
python3 -m unittest -v tests.test_approvals.CliTest tests.test_approvals.DoctorTest tests.test_doctor tests.test_path_resolution
python3 -m unittest discover -s tests && bash tools/check_generic.sh && bash tools/check_conventions.sh && echo ok
```

**Acceptance**

**A5 — the operator's commands and the doctor row.**

```bash
python3 -m unittest -v tests.test_approvals.CliTest tests.test_approvals.DoctorTest
```

## Acceptance
- [ ] 

## Non-goals

## History
- 2026-09-22: created (plan F-0031)
- 2026-09-23 00:05 ingest: state New → Active (branch worker/T-0019)
- 2026-09-23 00:52 ingest: state New → Active (branch worker/T-0019)
- 2026-09-23 00:53 adjudicate (adjudicate-t-0019): Nothing was under review — there is no `docs/reviews/1-t-0019.md` and no finding, so nothing is upheld or overruled. What was actually disputed was whether T-0019 could proceed at all: its step 1 precondition PD2 (`account_settings_path` on `origin/main`) is false, it is still false today, and three sessions in a row correctly stopped on it and therefore pushed nothing, which the factory read as an unpushed failure each time. That reading is what I overrule: a card blocked on one clause of one row is not a blocked card. What now holds is the standing rule that a session finishes every part of the scope that is not blocked and names what it left out — so T-0019 lands as `b90601e` with the whole of §2.5's operator surface (`asf approvals`, `list`, `resolve`) and the doctor's `approvals` row on its three clauses that need nothing from Task 3, and A5 passes green at 15 tests. What changes because of it: PD2 is demoted from a stop to a scope boundary for this card — it no longer gates T-0019, only the one `for` loop that the fourth clause will be, and this finding never returns in a later round. T-0019's remaining dependency is F-0031 Task 3's alone, and it is recorded in the code where the loop will go rather than in a hold. NEEDS OPERATOR: F-0031 Task 3 (`asf hooks install` puts the approvals hook in every worker account, plan `docs/plans/f-0031.md:193`) is the last thing between the doctor row and its fourth clause — confirm it is carded and sequenced, then have that card add the worker-account loop to `approvals.check_doctor`. Recommendation: let Task 3 own that loop, since it is the task that creates the entry the loop looks for.
- 2026-09-23 01:02 ingest: state Active → Closed (commit 6998049 names T-0019; CI green on main at or after it)

## Children

## Backlinks
- [F-0095](../features/F-0095.md) The wave relaunches coders on Tasks that end 'empty branch: nothing to land'
- [T-0020](../tasks/T-0020.md) The tick raises one line per held item and does not relaunch it
- [B-0076](../bugs/B-0076.md) A finished run with an empty branch blocks every task that waits on it — for ever
