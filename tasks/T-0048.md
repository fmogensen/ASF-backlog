---
id: T-0048
type: task
title: pool load counts every product's sessions and foreign ones, once each; the wave's running check is per product
parent: F-0076
decided: true
links: {plan: docs/plans/f-0076.md}
writes: [asf/workers/pool.py, asf/workers/wave.py, tests/test_session_identity.py, tests/test_workers.py]
# ---- machine ----
schema_version: 1
state: Closed
stage_since: 2026-09-23T12:57:28Z
cost: {sessions: 4, fix_rounds: 0, runner_min: 0, usd: 3.46}
evidence:
  - commit e15bbf1 names T-0048
  - CI green on main at or after it
  - "rule: landed-green"
updated: 2026-09-24T07:24:43Z
---
## Description
stories: S-8154
writes: asf/workers/pool.py, asf/workers/wave.py, tests/test_session_identity.py, tests/test_workers.py

Depends on T-9450 (`live_all`) and T-9451 (`observe.read`).

**Files**

- `asf/workers/pool.py`: `Pool.from_config`, `Pool.take`, `Pool.unreadable`; the module and
  class docstrings.
- `asf/workers/wave.py`: the running set and the D10 line; the module docstring.
- `tests/test_workers.py`: `Home.cfg` gains `'sessions': 'fake'` (D6). No assertion changes.
- `tests/test_session_identity.py`: `PoolAcrossProductsTest`.

**Steps**

1. `Pool.__init__` gains `unreadable=''` and stores it. A hand-built pool is readable.
2. `Pool.from_config(cfg, product, quota_source=None, session_source=None)`: `registered =
   lifecycle.live_all(os.path.join(env.ASF_HOME, 'state'))`, `observed, why =
   observe.read(cfg, accounts, source=session_source)`, then `extra` and `live` exactly as the spec.
   The dedupe keys are `{r['pid'] for r in registered}` and `{r.get('session') for r in registered} -
   {None}`. An observed session is extra only when its `account` is not None and it is in neither
   set. Every other constructor argument stays as it is. Keep the `guards=` line as the file has
   it when this Task starts (D13).
3. `Pool.take(account, model, job='', product=None)` records `product`. `load`, `under_caps`,
   `lane_load` and `pick_account` do not change, and an extra's `model: None` falls out of per-model
   caps as the spec says.
4. `wave`: `running = {(s.get('product') or product.name, s.get('job')) for s in pool.live}` (D5).
   A row waits `already running` when `(product.name, row.job)` is in it. After a launch, the pair
   is added and `pool.take(..., product=product.name)` is called. When `getattr(pool, 'unreadable', '')`
   is set, `out(f'pool: sessions unreadable ({why}) — counting registered sessions only')` runs once,
   before the loop.
5. `tests/test_workers.py` `Home.cfg['worker_pool']['sessions'] = 'fake'` (D6).
6. `PoolAcrossProductsTest` as below. A second product's registry is written with
   `pool_mod.append_session(env.Product('b', …), …)` under the same temp `ASF_HOME`.

**Gate**

```sh
python3 -m unittest tests.test_session_identity.PoolAcrossProductsTest tests.test_workers -v
python3 -m unittest discover -s tests -t . 2>&1 | tail -3
bash tools/check_generic.sh && bash tools/check_conventions.sh
```

**Acceptance**

- `PoolAcrossProductsTest(Home)`
  - `test_other_products_sessions_count`: acct-a `cap: 2`, product `b`'s registry has one live
    run on acct-a, the fake source shows it → `Pool.from_config(cfg, product_a).load(acct_a) == 1`.
  - `test_foreign_session_counts`: no registry runs, the fake source shows one foreign session on
    acct-a (`cap: 1`) → `pick_account` returns `(None, 'pool full')`.
  - `test_no_double_count`: a registered run and its observed process (same pid and id) → load 1.
  - `test_just_launched_counts_before_ps_sees_it`: a registered live run whose pid the source
    does not show → load 1.
  - `test_same_job_name_in_two_products`: product `b` has a live `spec-f-0001` → product `a`'s
    wave launches its own `spec-f-0001` (no `already running`).
  - `test_unreadable_prints_and_counts_registry`: source raises → the wave output has `pool:
    sessions unreadable`, and load equals the registered runs across products.

```sh
python3 -m unittest tests.test_workers tests.test_hermetic tests.test_metrics tests.test_views tests.test_lifecycle -v
```

## Acceptance
- [ ] 

## Non-goals

## History
- 2026-09-22: created (plan F-0076)
- 2026-09-23 10:48 adjudicate (adjudicate-t-0048): What was disputed was never the content of the work but whether T-0048 could be finished at all: three sessions produced the same correct edits and each died inside the >25-minute full suite before committing, so the branch came back empty three times and the record read it as a stalemate. What holds now: the hold is upheld as to fact — the work was real and unpushed, and it is committed and pushed as 49a264e. The blocking fact no round named is upheld as decisive — T-0048's step 2 depends on asf/workers/observe.py from T-9451, which exists on no branch of this repo and is not in this card's writes: footprint, so this card cannot create it and three of its six acceptance tests are unreachable by any amount of further effort; no correction round would have changed that, which is why the loop could not close itself. Overruled, and never to come back in a later round: that T-0048 is wholly blocked — the "counts every product's sessions" half of the item's own title needs only lifecycle.live_all (present at asf/workers/lifecycle.py:178), and it is delivered in 2988ca2 with PoolAcrossProductsTest covering every part reachable without observation, each test checked to fail against the trunk first. Also overruled: that the branch is red — the suite's 13 failures and 29 errors are byte-identical with the branch's files reverted to origin/main, are caused by those tests reading the operator's real ~/.ASF githooks, and are not this card's to fix. What changes: T-0048 carries steps 1, 3, 4, 5 and the registry half of step 2, green and pushed; its remainder is now explicitly T-9451's tail, recorded in from_config's own docstring so the next session finds it at the line rather than in a review file. NEEDS OPERATOR: T-0048 cannot close as fully done — its remaining acceptance depends on T-9451 (asf/workers/observe.py), which has not landed. Decide whether to close T-0048 at the registry half and move the three observation tests onto T-9451's card, or hold T-0048 open until T-9451 lands and re-queue it. Recommendation: close T-0048 and move them to T-9451 — that Task already writes tests/test_session_identity.py, so the tests land beside the code they exercise and nothing waits on a re-queue.
- 2026-09-23 11:19 ingest: state New → Active (branch worker/T-0048)
- 2026-09-23 12:57 ingest: state Active → Closed (commit e15bbf1 names T-0048; CI green on main at or after it)
- 2026-09-23 12:57 adjudicate (adjudicate-t-0048-correction): What was disputed was never the content of the work — three sessions produced the same correct edits and each died before committing, and round 1 then ruled the item half-deliverable because its step 2 depends on asf/workers/observe.py, which existed on no branch. That premise is no longer true and the finding is overruled: T-0047 landed observe at asf/workers/observe.py:192 (commit bebb641, now on the trunk), its read() returns each Observed with .account already resolved to the account *name* by _account_for, which is exactly what Pool.load compares at asf/workers/pool.py:252 — so the second half of step 2 was reachable the moment this worktree was rebased. Round 1's other finding, that the branch is red, is upheld as already-decided and stays overruled for good: clean origin/main at 449302e, carrying none of this branch's changes, fails with the identical failures=13/errors=6 in the identical four modules (test_install, test_doctor, test_sample_product, test_gitfixture), the visible cause being the operator's global core.hooksPath leaking past the hermetic build — none of it inside this card's footprint. The hold itself is upheld as to fact: the work was real and unpushed, and it is now committed as 29c0547. What holds now is that T-0048 is complete, not half-complete — steps 1 through 5 in full, with PoolAcrossProductsTest green on all twelve tests including test_foreign_session_counts, test_no_double_count and test_unreadable_prints_and_counts_registry, the three round 1 recorded as unreachable by any amount of effort. What changes: round 1's NEEDS OPERATOR is withdrawn as moot — nothing needs moving onto T-9451's card and nothing needs re-queueing, because the observation half landed here; and the real cause of the stalemate is named for the next card rather than this one — the gate template's bare `discover` line, not the work, is what three sessions died inside. NEEDS OPERATOR: the card gate template still prints `python3 -m unittest discover -s tests -t .`, which ran >22 min and was SIGKILLed (exit 137) in three consecutive T-0048 sessions — change the generated gate to `python3 tools/run_tests.py` (B-0068: same 1381 tests, hermetic, 296s), since the gate text lives in the record and its generator is outside any worker's footprint. NEEDS OPERATOR: main is red on its own — failures=13, errors=6 in test_install, test_doctor, test_sample_product, test_gitfixture, from the operator's global core.hooksPath (/Users/frank/.ASF/state/asf/githooks) surviving asf.hermetic.build — file a bug for it; I cannot mint the id, and recommend the hermetic build pin core.hooksPath=/dev/null the way tests/test_gitfixture.py:39 already expects.
- 2026-09-23 13:08 adjudicate (adjudicate-t-0048): The hold was not a review conflict — no `docs/reviews/1-t-0048.md` was ever written; T-0048 was held three times because its session died, so what was in dispute was only whether the work on the branch was in fact complete and faithful to F-0076, with nobody left to say so. It is, and it now holds. I checked every disputed anchor against the checkout rather than against either side's account. `Pool.from_config(cfg, product, quota_source=None, session_source=None)` sums `lifecycle.live_all(ASF_HOME/state)` across products and adds only those observed sessions whose account is not None and whose pid and `ASF_SESSION` both belong to no registered run, giving each seat exactly one count (spec D9); extras carry `model: None` and so count toward `cap` but fall out of the per-model ceiling; `unreadable` carries `why` and the wave prints the D10 line once before the loop. On the one place where plan and spec diverge I rule for the plan: the wave's running set is `{(s.get('product') or product.name, s.get('job'))}`, not the spec's bare `(s.get('product'), s.get('job'))`, because the bare form makes a hand-built `live` entry with no `product` never match and breaks the existing assertion at tests/test_workers.py:514-519 — D5 anticipated this and D12's intent, that `spec-f-0001` under product `b` never blocks `a`, is preserved either way. I also overrule, permanently, any objection that `Pool.from_config`'s dedupe keys deviate from the plan's literal `{r['pid'] for r in registered}`: the implementation's `{r.get('pid') ...} - {None}` is strictly safer, since a registry line without a pid would otherwise swallow an unrelated observed session, and it changes no specified behaviour. Neither of these findings returns in a later round. Nothing changes in the code as a result of this ruling; what changes is that T-0048's row closes as done rather than cycling into a fourth hold.

## Children

## Backlinks
- [T-0047](../tasks/T-0047.md) `asf.workers.observe` — every session on the machine, whose it is, which account
- [B-0083](../bugs/B-0083.md) A failed record step does not stop the tick: the wave acts on a stale board
- [B-0094](../bugs/B-0094.md) 19% of sessions end 'not pushed': finished work is lost to a missing commit or push
