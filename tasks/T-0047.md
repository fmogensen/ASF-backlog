---
id: T-0047
type: task
title: `asf.workers.observe` — every session on the machine, whose it is, which account
parent: F-0076
decided: true
links: {plan: docs/plans/f-0076.md}
writes: [asf/workers/observe.py, tests/test_session_identity.py, docs/config.example.yaml]
# ---- machine ----
schema_version: 1
state: Closed
stage_since: 2026-09-23T11:19:06Z
cost: {sessions: 5, fix_rounds: 0, runner_min: 0, usd: 4.27}
evidence:
  - commit bebb641 names T-0047
  - CI green on main at or after it
  - "rule: landed-green"
updated: 2026-09-24T07:24:43Z
---
## Description
stories: S-8153
writes: asf/workers/observe.py, tests/test_session_identity.py, docs/config.example.yaml

Depends on T-9450: `observe` parses the id with `lifecycle.parse_session`.

**Files**

- `asf/workers/observe.py` (new): `Observed`, `ObserveError`, `SessionSource`, `ProcessSource`,
  `CommandSource`, `FakeSource`, `source_from_config`, `read`, `identity_alive`.
- `tests/test_session_identity.py`: `ObserveTest`, plus a `fake_ps(directory, table)` helper that
  writes an executable `ps` stub printing `table`, or exiting 1. This is the `fake_launchctl`
  pattern (`tests/test_scheduler.py:63`). Tests put its directory first on `PATH` through
  `mock.patch.dict(os.environ, …)`.
- `docs/config.example.yaml`: the three `worker_pool` keys, commented.

**Steps**

1. `Observed = namedtuple('Observed', 'pid ppid account session product job owner cwd')`. `cwd` is
   None from every source here. The spec names the field and no source can read it with one `ps`
   call, so it stays None and nothing reads it.
2. `ProcessSource(binary, match)`: one `subprocess.run(['ps', 'axeww', '-o', 'pid=,ppid=,command='])`.
   `FileNotFoundError` or a non-zero exit → `ObserveError(<why>)`. Each line is `pid ppid rest`.
   It is a candidate when `re.search(match, rest)`, with `match` defaulting to
   `^(\S*/)?<binary>(\s|$)`. Drop a candidate whose ppid chain through the *full* table reaches
   another candidate, and guard the walk against cycles. The env comes from the `KEY=VALUE`
   tokens of `rest` for the five keys, and the last occurrence wins.
3. `CommandSource(cmd)`: `shlex.split`, run, one JSON object per non-empty line
   `{"pid", "ppid", "env": {…}}`. Non-zero exit or unparsable output → `ObserveError`.
   `FakeSource(entries)` returns those entries as they stand (D3).
4. `source_from_config(cfg)`: `worker_pool.sessions` (`process` default | `command` | `fake`).
   `command` with no `sessions_command` → `ObserveError` at read time, which is D10, not a crash
   at config load. `binary` = `worker_pool.binary` or `runtime.DEFAULT_BINARY`.
5. `read(cfg, accounts, source=None) -> ([Observed], why)`. The account is assigned by spec D7,
   in that order, with realpath on both sides (D11). `owner` is `asf` iff `ASF_SESSION` is
   set. `product`/`job` come from `parse_session`, else None. An `ObserveError` → `([], str(e))`,
   never raised.
6. `identity_alive(observed, runs)` exactly as the spec's bullet (D11 of the spec). Nothing calls
   it yet. T-9453 wires it in.
7. `docs/config.example.yaml`: under `worker_pool:`, after `accounts`, add the three keys commented
   out, with one comment line each: `sessions: process   # process | command | fake`,
   `sessions_command: …   # prints one JSON object per session: {"pid", "ppid", "env"}`, and
   `session_match: …   # default: the runtime binary as argv[0]`. Nothing else in the file moves.
8. `ObserveTest` as below.

**Gate**

```sh
python3 -m unittest tests.test_session_identity.ObserveTest -v
python3 -m unittest discover -s tests -t . 2>&1 | tail -3
bash tools/check_generic.sh && bash tools/check_conventions.sh
```

**Acceptance**

- `ObserveTest` (stub `ps`)
  - `test_sessions_and_their_children`: a table with a session (pid 100), its shell child (101)
    carrying the same env, and a nested agent (102, ancestor 100) → one `Observed`, pid 100.
  - `test_owner_and_account`: pid 100 with `ASF_SESSION=a/j@...` and
    `CLAUDE_CONFIG_DIR=<acct-a dir>` → `owner 'asf'`, `account 'acct-a'`, `product 'a'`. Pid 200
    with no `ASF_SESSION` and the same config dir → `owner 'foreign'`, `account 'acct-a'`. Pid 300
    with an unknown config dir → `account None`.
  - `test_command_source`: `worker_pool.sessions_command` printing two JSON lines → the same two
    `Observed`.
  - `test_unreadable`: the stub exits 1 → `read()` returns `([], '<why>')`, and does not raise.

```sh
python3 -m unittest tests.test_session_identity -v
```

## Acceptance
- [ ] 

## Non-goals

## History
- 2026-09-22: created (plan F-0076)
- 2026-09-23 10:35 adjudicate (adjudicate-t-0047): What was disputed was the gate: T-0047 was held three times on a red suite, with no review file ever written (docs/reviews/1-t-0047.md does not exist in any ref) and so no named finding against the code. The hold is misattributed and is OVERRULED. I exported origin/main — where asf/workers/observe.py does not exist — and ran the four red modules (test_doctor, test_install, test_sample_product, test_gitfixture) against that trunk: 13 failures and 6 errors, a byte-identical list of 19 test names to the branch's. The branch adds no failure and removes none. The cause is environmental and factory-wide: asf launches every worker session with core.hooksPath exported through GIT_CONFIG_* (the deliberate mechanism of docs/specs/f-0076.md D3), and those vars are command-line scope to git, outranking the repo-local core.hooksPath=/dev/null that tests/gitfixture.py:_disable_hooks writes to satisfy B-0073; with them cleared the full suite is green at 1375 tests OK. What now holds is the work as committed at 273b32a: I checked every anchor the card names against the checkout — Observed carries the eight named fields with cwd None, ProcessSource makes one ps axeww call with the default ^(\S*/)?<binary>(\s|$) match and a cycle-guarded ancestor drop, _parse_env takes the five keys with last-occurrence-wins, CommandSource raises ObserveError at read time for a missing sessions_command (D10) rather than at config load, FakeSource returns entries as they stand (D3), source_from_config defaults to process, and read() consumes lifecycle.parse_session, which returns (product, job, stamp) at asf/workers/lifecycle.py:96-107 so the parsed[0]/parsed[1] indexing is correct; ObserveTest and the fake_ps helper exist at tests/test_session_identity.py:246 and :30, and the item's 43 named tests pass. check_generic is clean and the three commented worker_pool keys are in docs/config.example.yaml. What changes because of this: nothing in the branch — no edit was upheld, so there is no commit, and T-0047 is landable as it stands. This overruled finding never comes back in a later round. The one real defect surfaced here — that the suite cannot pass inside an asf worker session because _disable_hooks does not neutralise the inherited GIT_CONFIG_* — belongs to tests/gitfixture.py, outside this item's writes footprint, and is raised as the operator line below rather than fixed here or given an invented id. NEEDS OPERATOR: the gate is red for every session on this machine, not just T-0047 — tests/gitfixture.py:_disable_hooks writes core.hooksPath=/dev/null to local config, which the session's inherited GIT_CONFIG_* overrides; cut a card (asf new) to make the suite's git calls clear it with the env -u GIT_CONFIG_COUNT idiom already at asf/workers/githooks.py:43, and until then run the gate as `env -u GIT_CONFIG_COUNT -u GIT_CONFIG_KEY_1 -u GIT_CONFIG_VALUE_1 -u GIT_CONFIG_KEY_0 -u GIT_CONFIG_VALUE_0 python3 tools/run_tests.py`. This likely also explains the T-0037, T-0040 and T-0048 sessions sitting in adjudicate.
- 2026-09-23 11:19 ingest: state New → Closed (commit bebb641 names T-0047; CI green on main at or after it)

## Children

## Backlinks
- [T-0048](../tasks/T-0048.md) pool load counts every product's sessions and foreign ones, once each; the wave's running check is per product
- [B-0082](../bugs/B-0082.md) A timed-out gate is counted as a failed round and sends the item to Opus
- [B-0083](../bugs/B-0083.md) A failed record step does not stop the tick: the wave acts on a stale board
