---
id: T-0026
type: task
title: no session is launched into a repo with no push gate — `asf/workers/spawn.py`
parent: F-0075
decided: true
links: {plan: docs/plans/f-0075.md}
writes: [asf/workers/spawn.py, tests/test_workers.py]
after: [T-0025]   # T-0025 creates asf.hooks.ensure_git_hooks, which this calls
# ---- machine ----
state: Closed
stage_since: 2026-09-23T06:43:44Z
cost: {sessions: 9, fix_rounds: 0, runner_min: 0, usd: 7.53}
evidence:
  - commit fb58ca9 names T-0026
  - CI green on main at or after it
  - "rule: landed-green"
updated: 2026-09-24T07:24:43Z
---
## Description
stories: S-7054
writes: asf/workers/spawn.py, tests/test_workers.py

**Files**

- `asf/workers/spawn.py` — `make_worktree`
- `tests/test_workers.py`

**Steps**

1. `make_worktree(product, job, branch)` calls `hooks.ensure_git_hooks(product)` **first** — after
   the `repo_dir` check and before `lifecycle.may_launch` or any of its four paths: the worktree of
   an ended run it takes over as it stands (B-0025, B-0051), a worktree on a held branch already on
   origin (B-0046, B-0048), a local branch with no worktree that is reused when it carries nothing
   and refused with its commit count when it does (B-0025), and a fresh branch off the trunk. One
   call before the branching is what covers all four: no path may reach `git worktree add` without
   the hooks in place (D10).
2. Missing hooks are written. A foreign hook in the way raises `SpawnError` carrying the same
   `NEEDS OPERATOR` line `ensure_git_hooks` produced, and the wave does not launch that row.
3. `tests/test_workers.py` gains `SpawnHookTests`, per §3.4 — including the two reuse paths, which
   are the ones a naive placement after the branching would miss.

**Gate**

```sh
python3 -m unittest discover -s tests
bash tools/check_generic.sh
bash tools/check_conventions.sh
```

**Acceptance**

```sh
python3 -m unittest -v \
  tests.test_workers.SpawnHookTests.test_spawn_installs_missing_hooks_before_the_worktree \
  tests.test_workers.SpawnHookTests.test_spawn_refuses_to_launch_past_a_foreign_hook \
  tests.test_workers.SpawnHookTests.test_a_reused_ended_worktree_gets_the_hooks_too \
  tests.test_workers.SpawnHookTests.test_a_reused_local_branch_with_no_worktree_gets_the_hooks_too
```

## Acceptance
- [ ] 

## Non-goals

## History
- 2026-09-22: created (plan F-0075)
- 2026-09-22 15:48 ingest: state New → Active (branch task/T-0026)
- 2026-09-22 18:36 adjudicate (adjudicate-t-0026): What was disputed is whether make_worktree's push-gate call should be unconditional or should skip itself when asf.hooks.ensure_git_hooks is absent — the working tree held an uncommitted softening to getattr(hooks, 'ensure_git_hooks', None), and the branch was held three times because no session would commit it. The softening is OVERRULED and has been reverted: it is fail-open on the one gate this item exists to install, and it contradicts both the item's own title and D10 as the plan states it at line 341, "no path may reach git worktree add without the hooks in place"; it bought a green suite by deleting the feature, and it does not come back in a later round. The unconditional call at asf/workers/spawn.py:160 is UPHELD exactly as committed in 16c99bd, which is plan step 1 verbatim. What now holds is that the stalemate was never a code-design question at all: asf/hooks.py has no ensure_git_hooks, and docs/plans/f-0075.md:15,17 queue Task 8354 behind Task 8353 precisely because 8353 creates it — T-0026 was launched out of order, so the six test_sample_product failures are the dependency's absence, not a defect in this diff, and I confirmed this by running both variants. What changes is that no further correction round is spent on spawn.py: the branch stands as pushed, it is expected to be red until Task 8353 lands, and the remaining work is a sequencing decision that belongs to the operator, since fail-closed and a green suite are mutually exclusive on this branch and no edit inside T-0026's footprint can reconcile them without abandoning D10. NEEDS OPERATOR: Task 8353 (F-0075, asf/hooks.py — ensure_git_hooks) must land before T-0026 merges — land 8353, then rebase task/T-0026 onto it and re-run the gate; do not merge T-0026 first.
- 2026-09-23 06:43 ingest: state Active → Closed (commit fb58ca9 names T-0026; CI green on main at or after it)
- 2026-09-23 06:53 adjudicate (adjudicate-t-0026-correction): What was disputed is whether T-0026's change is defective, having been held three times on a red gate. It is not. The branch's diff matches plan Task 8354 steps 1-3 exactly: the call to hooks.ensure_git_hooks sits at asf/workers/spawn.py:159-161, after the repo_dir check and above lifecycle.may_launch (:166) and all four worktree paths, so no path reaches git worktree add without the gate (D10); SpawnHookTests covers both reuse paths and all four acceptance tests pass. The gate is nonetheless red, and genuinely so — 1 failure and 5 errors, every one of them tracing to the single line "[step:wave] FAILED module 'asf.hooks' has no attribute 'ensure_git_hooks'". The cause is sequencing, not code: docs/plans/f-0075.md wave order makes T-0026 Task 8354 in wave 3, dependent on 8353 which creates ensure_git_hooks, under the plan's own rule that "nothing below it may start before it lands" — yet asf/hooks.py has no ensure_git_hooks (grep finds it only in T-0026's own commit, the plan and the spec) and asf/redact.py, all of wave 1, does not exist at all. tests/test_sample_product passes clean on origin/main and fails only with this commit, which confirms the attribution without excusing the redness: T-0026 is complete and correct but unlandable, because landing it would break the trunk until its dependency arrives. What now holds: the three corrections are overruled and that finding never returns; the red gate is upheld as a fact but is not a defect of this item, and no fourth correction round is warranted, because no edit within asf/workers/spawn.py or tests/test_workers.py can turn the suite green — the only two that would are implementing another task's footprint, or degrading the call to a getattr fallback that would silently launch sessions into ungated repos and defeat the very guarantee F-0075 exists to give. What changes: T-0026 stops being treated as a failing item and becomes a blocked one, held until Task 8353 lands rather than re-corrected; on that rebase the blanket mock.patch.object(hooks_mod, 'ensure_git_hooks', create=True) in tests/test_workers.py Home.setUp must lose its create=True, which is load-bearing only while the real function is absent and would otherwise mask exactly this class of failure ever after. NEEDS OPERATOR: T-0026 is blocked on unlanded wave-1 and wave-2 dependencies — land Task 8350 (asf/redact.py) and Task 8353 (hooks.ensure_git_hooks), then rebase task/T-0026; its gate goes green with no change to its diff.

## Children

## Backlinks
- [F-0088](../features/F-0088.md) Order is a derived relation: the plan states it, the planner writes it, preflight enforces it
- [F-0090](../features/F-0090.md) Adjudication is the last resort: the cheap causes are ruled out before Opus is spent
- [B-0079](../bugs/B-0079.md) A branch ahead of the trunk whose run failed is orphaned: harvest never looks at it again
- [B-0080](../bugs/B-0080.md) after: holds the coder row only — correction and adjudicate launch anyway, on Opus
- [B-0094](../bugs/B-0094.md) 19% of sessions end 'not pushed': finished work is lost to a missing commit or push
