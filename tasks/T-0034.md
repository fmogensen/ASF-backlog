---
id: T-0034
type: task
title: `asf inbox` — one untyped card into the record's intake directory
parent: F-0073
decided: true
links: {plan: docs/plans/f-0073.md}
writes: [asf/groom/inbox.py, asf/cli.py, tests/test_inbox_shape.py, tests/test_path_resolution.py]
# ---- machine ----
state: Closed
stage_since: 2026-09-23T12:57:28Z
cost: {sessions: 3, fix_rounds: 0, runner_min: 0, usd: 2.87}
evidence:
  - commit 11e88a8 names T-0034
  - CI green on main at or after it
updated: 2026-09-23T12:57:33Z
---
## Description
stories: S-7953
writes: asf/groom/inbox.py, asf/cli.py, tests/test_inbox_shape.py, tests/test_path_resolution.py

**Files**: `asf/groom/inbox.py` (adds `cmd_inbox`), `asf/cli.py`, `tests/test_inbox_shape.py`
(adds `InboxCommandTest`), `tests/test_path_resolution.py` (the PD12 row).

**Steps**
1. `cli.py`:
   - Add the subparser `inbox` with
     `help="file an untyped card into the record's intake dir (the groom types it)"` and the
     arguments `--title` (required), `--body-file`, `--parent` and `--product`. There is no
     `--type`.
   - Dispatch with
     `cmd_inbox(args, resolve_record(args, announce=_announce_stderr))`.
2. `cmd_inbox(args, root)`:
   - `d = os.path.join(root, intake_dir(args))`, created if it is missing.
   - The slug is `re.sub(r'[^a-z0-9]+', '-', title.lower()).strip('-')`, or `card` if that is
     empty. If the name is taken, append `-2`, `-3`, and so on.
   - The text is `# {title}\n`, then `parent: {id}\n` if given, then `\n`, then the body file
     verbatim.
   - Print the path relative to `root` and return 0. It mints nothing and runs no groom.
3. `InboxCommandTest`: every case the spec lists. `test_writes_an_untyped_card` compares a
   before/after listing of every item folder. `test_inbox_then_groom` runs `asf inbox --title
   "Billing invoices" --parent E-0001` and then `asf groom`, and expects exactly one new file
   under `features/` whose `parent` is `E-0001`.
4. `tests/test_path_resolution.py`: add the PD12 row.
5. Nothing is added to `asf.plugin_build`, because `inbox` is neither a view nor a dialogue.
   `python3 -m asf.plugin_build check` must stay clean.

**Gate**
```bash
python3 -m unittest -v tests.test_inbox_shape.InboxCommandTest tests.test_path_resolution tests.test_plugin
python3 -m unittest discover -s tests -t . 2>&1 | tail -3
bash tools/check_generic.sh
python3 -m asf.plugin_build check
```

**Acceptance**
```sh
python3 -m unittest tests.test_inbox_shape -v
```
- S-7953 — `python3 -m unittest tests.test_inbox_shape.InboxCommandTest -v`

---

## Acceptance
- [ ] 

## Non-goals

## History
- 2026-09-22: created (plan F-0073)
- 2026-09-23 11:19 ingest: state New → Active (branch worker/T-0034)
- 2026-09-23 12:57 ingest: state Active → Closed (commit 11e88a8 names T-0034; CI green on main at or after it)
- 2026-09-23 12:57 adjudicate (adjudicate-t-0034-correction): Three holds were in dispute on worker/T-0034, and all three are overruled as spent. The two "unpushed work" holds (rounds 1 and 2) are overruled: the work is committed and signed off at 9f12366, its four-file tree is already on origin as f6a6dad, and git status is clean — the only residue was an empty untracked .branch_fails.txt left by the adjudicate session that was killed, which I deleted. The round-3 hold "conflict in asf/groom/inbox.py" is overruled: git merge-tree --write-tree origin/main HEAD exits 0 and writes tree 8dc8793, so no conflict survives. The collision was T-0034's cmd_inbox landing on the same region T-0040 rewrote, and the rebase onto df8af43 already resolved it — git show HEAD -- asf/groom/inbox.py is 40 added lines with zero deletions, so none of T-0040's intake_dir work was lost, and the resolution additionally fixed a latent shadowing defect by renaming the module-level helper intake_dir (f6a6dad:asf/groom/inbox.py:13, colliding with process_inbox's own keyword parameter at :102) to _intake_dir (asf/groom/inbox.py:12). The branch's red full suite is likewise not a hold on this item: 13 failures and 6 errors, every one reproduced name-for-name on origin/main at 07e130a with none of T-0034's code present, all stemming from this machine's githooks/pre-commit not matching what asf install recognises; that is a defect on main and is raised as a NEEDS OPERATOR line, not charged to T-0034. What now holds: T-0034's implementation stands as committed at 9f12366, its own gate is green (17 named tests OK, check_generic clean, plugin_build up to date, check_conventions clean), and none of these three findings may be raised again in a later round. What changes: nothing in the code — no edit was warranted, so there is no new commit; the branch is complete and waits only on the factory to publish the rebased lane, since git push origin worker/T-0034 is refused non-fast-forward exactly as B-0056 describes. NEEDS OPERATOR: 19 pre-existing test failures on origin/main on this machine — `asf install` does not recognise the `asf-hook` shim at /Users/frank/.ASF/state/asf/githooks/pre-commit as asf's; file it via `asf inbox --title "asf install does not recognise the asf-hook shim it installed" --parent E-0001` with a signature line rather than hand-editing the hooks

## Children

## Backlinks
- [B-0083](../bugs/B-0083.md) A failed record step does not stop the tick: the wave acts on a stale board
