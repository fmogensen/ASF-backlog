---
id: T-0018
type: task
title: `asf hooks install` puts the approvals hook in every worker account
parent: F-0031
decided: true
links: {plan: docs/plans/f-0031.md}
writes: [asf/hooks.py, tests/test_install.py, tests/test_approvals.py]
after: [T-0017]
# ---- machine ----
state: Closed
stage_since: 2026-09-23T01:45:26Z
cost: {sessions: 6, fix_rounds: 0, runner_min: 0, usd: 2.14}
evidence:
  - commit 5583038 names T-0018
  - CI green on main at or after it
updated: 2026-09-23T01:45:26Z
---
## Description
stories: S-7452
writes: asf/hooks.py, tests/test_install.py, tests/test_approvals.py

**Files**
- `asf/hooks.py` — `hook_command`/`_is_ours` product-less form, `account_settings_path`,
  `install` writing the worker accounts.
- `tests/test_install.py` — `HooksTest` updated to the new message and the always-present
  approvals entry (PD6).
- `tests/test_approvals.py` — `HookTest.test_install_writes_the_entry_into_every_worker_account`.

**Steps**
1. PD2: `git show origin/main:asf/hooks.py | grep -q "BUILTIN"`; else the NEEDS OPERATOR line
   and stop.
2. `hook_command(asf_path, name, product)`: `product is None` → `<asf> hook <name>`.
   `_is_ours(command, name, product)`: `product is None` → `(^|/)asf hook <name>$`. `merge`
   passes `product` through unchanged.
3. `account_settings_path(account, home=None)` (PD4): `<expanduser(config_dir)>/settings.json`
   when set, else the runtime's user settings file under `account.home`, else under
   `home or os.path.expanduser('~')`.
4. `install(product, rules_dir, which, cfg=None)`: rule hooks go to the product repo exactly as
   today when any rule declares one (PD5); then `('PreToolUse', 'approvals')` is merged,
   product-less, into `account_settings_path(a)` for every `pool.accounts_from_config(cfg or
   env.load_config())`, creating the directory, idempotent through `merge`. Message:
   `hooks: <n> rule hooks in <repo settings>; approvals in <k> worker accounts`.
5. `tests/test_install.py` `HooksTest`: write an empty `config.yaml` in the temp home so no real
   account is touched; replace the `0 rules declare a hook` and `already installed` assertions
   with the new message and file-content idempotence (PD6); keep every other assertion.
6. The install test (§3 A2): two accounts with `config_dir` in a temp `config.yaml`, one settings
   file with an unrelated key that survives, a second install leaves both files byte-identical,
   and the entry is `{"matcher": "*", "hooks": [{"type": "command", "command": "<asf> hook approvals"}]}`.

**Gate**
```bash
python3 -m unittest -v tests.test_approvals.HookTest tests.test_install.HooksTest
python3 -m unittest discover -s tests && bash tools/check_generic.sh && bash tools/check_conventions.sh && echo ok
```

**Acceptance**

and the unit cases behind it:

```bash
python3 -m unittest -v tests.test_approvals.HookTest
```

## Acceptance
- [ ] 

## Non-goals

## History
- 2026-09-22: created (plan F-0031)
- 2026-09-22 23:04 ingest: state New → Active (branch worker/T-0018)
- 2026-09-23 01:33 ingest: state New → Active (branch worker/T-0018)
- 2026-09-23 01:33 adjudicate (adjudicate-t-0018): What was disputed was nothing in the code — there is no round-1 review on this branch (docs/reviews/ holds only 1-b-0005-ruling.md), and no finding was ever written. The single open hold was procedural and factually correct: three sessions produced the complete Task 3 diff in the worktree and each ended without committing it, so origin/worker/T-0018 did not exist and the gate held the branch "unpushed" three times (B-0051), which the record read as a stalemate. That hold is upheld, not overruled — the work genuinely was unpushed — and its exact remedy was to commit the working tree as it stood and push it, which I have done at 6fab061. On the substance I checked every disputed anchor against the checkout myself before landing it: PD2's precondition holds (origin/main:asf/hooks.py names BUILTIN); hook_command/_is_ours gain a product-less form whose `$`-anchored pattern cannot collide with a product rule entry; account_settings_path reads CLAUDE_CONFIG_DIR as replacing the runtime's settings directory rather than parenting it, per PD4; install drops the "0 rules declare a hook" early return and narrows the no-repo_dir refusal to the case where a rule actually declares a hook, per PD5; and the two test files carry PD6's new message and byte-level idempotence plus the §3 A2 two-account install case. The gate is green — 1082 tests OK, check_generic clean. What changes is that F-0031's Task 3 is now on a published lane branch for harvest instead of being rewritten a fourth time; no finding was overruled here, because none was ever raised, and the adjudication loop on T-0018 ends with this ruling. Note for the factory: origin/main advanced to 448a676 (T-0020) while this session ran, so the branch is ahead 1 / behind 1 and needs the usual rebase at harvest — I did not merge, rebase or force-push it.
- 2026-09-23 01:45 ingest: state Active → Closed (commit 5583038 names T-0018; CI green on main at or after it)

## Children

## Backlinks
- [F-0095](../features/F-0095.md) The wave relaunches coders on Tasks that end 'empty branch: nothing to land'
- [T-0019](../tasks/T-0019.md) The operator's side — `asf approvals`, `list`, `resolve`, and the doctor row
- [T-0020](../tasks/T-0020.md) The tick raises one line per held item and does not relaunch it
- [B-0076](../bugs/B-0076.md) A finished run with an empty branch blocks every task that waits on it — for ever
