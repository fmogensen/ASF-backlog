---
id: T-0041
type: task
title: The sample product differs on every kind of convention, and CI reads the result
parent: F-0074
decided: true
links: {plan: docs/plans/f-0074.md}
writes: [sample/product.yaml, sample/repo/reviews/README.md, sample/repo/specs/f-0002.md, sample/README.md, tests/test_sample_product.py]
# ---- machine ----
schema_version: 1
state: Closed
stage_since: 2026-09-23T05:52:18Z
cost: {sessions: 4, fix_rounds: 0, runner_min: 0, usd: 11.02}
evidence:
  - commit a61b3bb names T-0041
  - CI green on main at or after it
  - "rule: landed-green"
updated: 2026-09-24T07:24:43Z
---
## Description
stories: S-8005
writes: sample/product.yaml, sample/repo/reviews/README.md, sample/repo/specs/f-0002.md, sample/README.md, tests/test_sample_product.py

**Files**
- `sample/product.yaml` — three new `conventions:` keys.
- `sample/repo/reviews/README.md` (new, one line, so the directory exists on the sample's trunk).
- `sample/repo/specs/f-0002.md` (new, ~ten lines: F-0002's spec, so the evidence pass has a spec
  to find under `specs/`).
- `sample/README.md` — the conventions table gains the three rows.
- `tests/test_sample_product.py` — four new cases.

**Steps**
1. `sample/product.yaml` gains `reviews_dir: reviews`, `review_pattern: reviews/{slug}-r{n}.md`
   and `intake_dir: cards` under `conventions:`. `specs_dir`, `plans_dir`, `default_bug_epic` and
   `branch_prefixes` are already there and unchanged. Note the sample's review pattern puts the
   round *after* the slug — the opposite order to the default — so a copy of the default cannot
   satisfy it.
2. The two new repo files, and the three rows in `sample/README.md`'s table.
3. `test_init_creates_the_intake_dir_not_inbox`: the record clone has `cards/`, and no `inbox/`.
4. `test_the_spec_brief_cites_the_samples_dirs`: `asf brief --product sample --item F-0001` → exit
   0; the body contains ``Specs live in `specs`, plans in `plans`, reviews in `reviews`.``,
   `specs/f-0001.md` and `reviews/f-0001-r1.md`, and never `docs/specs` or `docs/reviews`.
5. `test_evidence_sees_the_spec_under_specs`: `asf evidence --product sample --fresh --json` →
   exit 0, `features['f-0002']['spec_doc'][0] == 'origin/main:specs/f-0002.md'`; the output names
   no brief tree, no decisions file and no parity matrix (the sample declares none, so nothing
   looked for them).
6. `test_the_evidence_cache_is_under_the_products_state`: `<home>/state/sample/cache-evidence.json`
   exists after that call, and nothing was written under a `TMPDIR` the test owns.
7. `test_doctor_is_clean` is unchanged and now runs over the new conventions.

**Gate**
```bash
python3 -m unittest -v tests.test_sample_product
python3 -m unittest discover -s tests
bash tools/check_generic.sh && bash tools/check_conventions.sh && asf plugin check
```

**Acceptance**
```sh
python3 -m unittest -v tests.test_sample_product
```

---

## Acceptance
- [ ] 

## Non-goals

## History
- 2026-09-22: created (plan F-0074)
- 2026-09-22 16:53 ingest: state New → Active (branch task/T-0041)
- 2026-09-22 18:52 adjudicate (adjudicate-t-0041): The stalemate was naming, and both sides were partly right, so the file decided each anchor separately. UPHELD, and fixed: the cache assertion named cache-backlog-evidence.json, but _cache_file joins "cache-" to a base of "evidence.json" (asf/evidence/evidence.py:70,1018), so the path is cache-evidence.json — the name the plan's step 6 and the spec's line 452 already carried; the gate was genuinely red on this and is now green. UPHELD as to the name only: test_the_spec_brief_cites_the_samples_dirs drove --kind review, which contradicted its own name; the case is renamed test_the_review_brief_cites_the_samples_dirs. UPHELD and added: the cache case now owns a TMPDIR and asserts nothing was written there, and the evidence case asserts superpowers, sdd-input and feature-matrix are absent — both were the spec's stated acceptance and had been dropped. OVERRULED, and these never come back in a later round: the plan's asf evidence --json is stale — asf/cli.py:100 gives p_evidence only --fresh; the plan's features['f-0002']['spec_doc'][0] is stale — asf/evidence/evidence.py:610 serialises the key as "spec" holding a plain string, which is what the coder wrote and what passes; and the coder's substitution of --kind review for the plan's bare brief invocation stands, because asf/briefs/preamble.py:348 emits the review path only if kind in REVIEW_KINDS, so no spec-kind body can satisfy step 4's two assertions at once — the plan's step 4 is wrong on this point, not the branch. OVERRULED as a finding against this session, but recorded as debt: the fourth case, test_init_creates_the_intake_dir_not_inbox, cannot be written green today on two independent counts I verified — nothing reads conventions.intake_dir (the field is at asf/conventions.py:100 and appears nowhere under asf/groom, asf/record or asf/tick, so Task 5 is unlanded), and this fixture's asf init takes the adopt branch at asf/init.py:275, so lay_down, the only caller that creates the stream folders, never runs here, as test_init_adopts_the_ten_cards proves. Writing it red would fail the gate and dropping it silently would lose F-0074's PD5 proof, so it lands skipped with both blockers named in the skip reason. What changes: T-0041's branch is gate-green and lands as it stands; Task 5 of F-0074 inherits the skipped case and must un-skip it, and whoever revisits the plan should correct step 4's bare brief invocation, step 5's --json and spec_doc[0], since the code has moved past all three.
- 2026-09-23 05:52 ingest: state Active → Closed (commit a61b3bb names T-0041; CI green on main at or after it)

## Children

## Backlinks
- [B-0079](../bugs/B-0079.md) A branch ahead of the trunk whose run failed is orphaned: harvest never looks at it again
