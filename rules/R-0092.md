---
id: R-0092
type: rule
title: Never source the local env file directly; load env through the scripts
scope: session
source: migrated from the first product's backlog
enforced: false
reason: check to be ported
# ---- machine ----
state: New
stage_since: 2026-09-21T14:00:00Z
updated: 2026-09-21T14:00:00Z
---
## Statement
`.env.local` is never sourced in a shell. Environment comes through the test-database wrapper and its siblings, which read the file without echoing it.

## Why
A zsh parse error printed the hosting provider token into a task output.

## Check
None yet. The source check for this rule sources a shared library bound to one product's repository, state directory, branch naming and scheduled job, so it cannot be carried as it stands. It becomes a check when the rule runner reads those four facts from the product's configuration.

## Source
Migrated from the first product's backlog, 2026-09-21.

## History
- 2026-09-21 migrated from the first product's backlog

## Children

## Backlinks
