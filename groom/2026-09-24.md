# Groom 2026-09-24

## Inbox cards to decide

- [ ] B-0102 asf groom / asf set write the checkout and never push — edits drift from the tick clone — from inbox as bug (signature), awaiting a decision → answer: ____
- [ ] B-0103 asf init writes a pre-commit that doctor then calls foreign — a new product starts RED — from inbox as bug (signature), awaiting a decision → answer: ____
- [ ] B-0104 briefs: spec/plan templates don't state the commit-subject rule harvest enforces — from inbox as bug (signature), awaiting a decision → answer: ____
- [ ] B-0105 Config reader: a YAML flow-style map loads silently as a string — parse it or reject it clearly — from inbox as bug (signature), awaiting a decision → answer: ____
- [ ] B-0106 Failed tick step logs no traceback — from inbox as bug (signature), awaiting a decision → answer: ____
- [ ] B-0107 feeder: a pushed plan/spec awaiting its PR is relaunched as STARVED → PLAN/SPEC — from inbox as bug (signature), awaiting a decision → answer: ____
- [ ] B-0108 Flow-style config maps load as strings; readers silently fall back — from inbox as bug (signature), awaiting a decision → answer: ____
- [ ] B-0109 Full test suites run locally in parallel and exhaust the host (load 99, swap 95%) — from inbox as bug (signature), awaiting a decision → answer: ____
- [ ] B-0110 Harvest re-gates a green code branch when main moved only by docs commits — from inbox as bug (signature), awaiting a decision → answer: ____
- [ ] B-0111 Inbox intake drops headers after an unknown key; a Bug card became a Feature — from inbox as bug (signature), awaiting a decision → answer: ____
- [ ] B-0112 install.sh reports step 4 FAILED for scheduler.kind cron, which is the expected outcome — from inbox as bug (signature), awaiting a decision → answer: ____
- [ ] B-0113 install.sh step 1 failure exits with pipx's code and no NEEDS OPERATOR guidance — from inbox as bug (signature), awaiting a decision → answer: ____
- [ ] B-0114 native landing: a merged spec/plan PR marks its Feature landed/Resolved, so no coder ever launches — from inbox as bug (signature), awaiting a decision → answer: ____
- [ ] B-0115 scheduler install runs the daily clock at load, hours outside its declared time — from inbox as bug (signature), awaiting a decision → answer: ____
- [ ] B-0116 Spec/plan approval ignores review_pattern: one review-file contract for every kind — from inbox as bug (signature), awaiting a decision → answer: ____
- [ ] B-0117 Status Prod row names ci.deploy_workflow but the product schema rejects it — from inbox as bug (signature), awaiting a decision → answer: ____
- [ ] B-0118 status: the Prod row reads ci.deploy_workflow, not deploy_sha.workflow — from inbox as bug (signature), awaiting a decision → answer: ____
- [ ] B-0119 tick logs stay empty while steps run; a live tick looks the same as a hung one — from inbox as bug (signature), awaiting a decision → answer: ____
- [ ] F-0108 Release ASF 0.1 for feedback: general installer, first-user docs, release notes, a feedback channel — from inbox as feature (default), awaiting a decision → answer: ____
- [ ] F-0112 Products update themselves to a new ASF release: upgrade: auto in the tick, with rollback on a red doctor — from inbox as feature (default), awaiting a decision → answer: ____
- [ ] F-0115 Worker sessions are refused the repo's own lint (check_conventions) and printenv — from inbox as feature (default), awaiting a decision → answer: ____
- [ ] F-0127 A product declares a worktree setup command; the spawner runs it in every fresh worktree — from inbox as feature (default), awaiting a decision → answer: ____
- [ ] F-0128 approvals: decide_feature: auto — ASF decides a product's Features itself (operator instruction) — from inbox as feature (default), awaiting a decision → answer: ____
- [ ] F-0129 asf config check/migrate: an old or hand-made product yaml maps onto the schema without a hand rewrite — from inbox as feature (default), awaiting a decision → answer: ____
- [ ] F-0130 backfill: reconcile a partly-built product's history into the record (asf backfill) — from inbox as feature (default), awaiting a decision → answer: ____
- [ ] F-0131 Classify every cancelled CI job as timeout, runner loss or failure, and list jobs near their limit — from inbox as feature (default), awaiting a decision → answer: ____
- [ ] F-0132 Derived backlinks copy a protected name from one card's title into others, and redaction then refuses unrelated pushes — from inbox as feature (default), awaiting a decision → answer: ____
- [ ] F-0133 Groom commits its own output; a foreign daily stamp never skips the native daily; new products groom by default — from inbox as feature (default), awaiting a decision → answer: ____
- [ ] F-0134 Intake silently ignores an explicit type: line — an operator's 'type: bug' card is minted as a Feature — from inbox as feature (default), awaiting a decision → answer: ____
- [ ] F-0135 Native PR landing: merge an ASF-opened PR when approved and green, so PR products need no merge-queue script — from inbox as feature (default), awaiting a decision → answer: ____
- [ ] F-0136 No command pauses a product's clock for a rollout — from inbox as feature (default), awaiting a decision → answer: ____
- [ ] F-0137 Pause one product: the tick keeps recording and harvesting but launches nothing — from inbox as feature (default), awaiting a decision → answer: ____
- [ ] F-0138 Predictive capacity: place a launch on the account that can finish it, by provider kind — from inbox as feature (default), awaiting a decision → answer: ____
- [ ] F-0139 Status Record row: calculate the open split (work, Resolved, records) instead of one total — from inbox as feature (default), awaiting a decision → answer: ____
- [ ] F-0140 The factory's own clock imports from the live dev checkout — a tick mid-update gets a torn tree; tick from a snapshot — from inbox as feature (default), awaiting a decision → answer: ____
- [ ] F-0141 The landing gate refuses a branch that adds a debug-toggle marker — from inbox as feature (default), awaiting a decision → answer: ____


## Undecided > 3 days

- [ ] E-0003 ASF 0.3 — autonomous delivery — undecided 3d → answer: ____ (barred: approvals.new_epic)
- [ ] F-0010 P3 wave 2 — evidence and ingest — undecided 3d → answer: ____
- [ ] F-0013 P3 wave 5 — the feeder and the tick — undecided 3d → answer: ____
- [ ] F-0014 P3 wave 6 — the CLI and the plugin — undecided 3d → answer: ____
- [ ] F-0021 P5 — launch — undecided 3d → answer: ____
- [ ] F-0042 Credentials as a rule: a daily check that every signed-in tool is valid for three more days — undecided 3d → answer: ____
- [ ] F-0051 The production verification probe: a real customer journey after every deploy — undecided 3d → answer: ____
- [ ] F-0052 Budget enforcement per Epic — undecided 3d → answer: ____
- [ ] F-0062 The launch path: skills as procedures, role agents with restricted tools, per-job caps, deny rules — undecided 3d → answer: ____
- [ ] F-0068 Spike: one Feature through a hosted agent runtime, compared with the local pool — undecided 3d → answer: ____
- [ ] F-0069 Security checks by code: a review pass on sensitive paths, secret and dependency scanning as rules — undecided 3d → answer: ____


## Features without Stories

- [ ] F-0021 P5 — launch — no Stories → answer: ____
- [ ] F-0023 The grill: a request is interrogated before it is spent on — no Stories → answer: ____
- [ ] F-0024 Self-amendment policy: the factory proposes, humans approve and merge — no Stories → answer: ____
- [ ] F-0025 Typed envelopes: every job ends with a machine-readable report beside its prose — no Stories → answer: ____
- [ ] F-0026 The writes boundary holds a branch; it never silently reverts — no Stories → answer: ____
- [ ] F-0029 Roles: one file per role, five sections, model and access from config — no Stories → answer: ____
- [ ] F-0030 The README carries the argument, the mental model and the manual on one page — no Stories → answer: ____
- [ ] F-0033 Dogfood end to end in CI — no Stories → answer: ____
- [ ] F-0035 Thin controller: every read loop moves to the tick, and a rule flags scriptable chores — no Stories → answer: ____
- [ ] F-0038 A factory-only CI class: a push touching only factory code runs lint and the factory tests — no Stories → answer: ____
- [ ] F-0039 Same-session correction: review findings go back to the writer's own session; a fixer only for a dead one — no Stories → answer: ____
- [ ] F-0040 Story-to-test gate: a Task's pull request must tick an acceptance line with the test that proves it — no Stories → answer: ____
- [ ] F-0041 Small-Task trains: size class from the footprint, one train per lane, one CI run and one review per train — no Stories → answer: ____
- [ ] F-0042 Credentials as a rule: a daily check that every signed-in tool is valid for three more days — no Stories → answer: ____
- [ ] F-0043 CI waste targets on the scorecard, and a week over target files a Bug — no Stories → answer: ____
- [ ] F-0044 The retro's first line: features on production per week, and cost per feature — no Stories → answer: ____
- [x] F-0046 The conflicts pass: supersession enforced, same-subject rule and decision pairs into the groom — no Stories → answer: (spoken for: STARVED → PLAN)
- [x] F-0050 The product improvement loop: the factory proposes product changes from the product's own measurements — no Stories → answer: (spoken for: CARD → SPEC)
- [ ] F-0051 The production verification probe: a real customer journey after every deploy — no Stories → answer: ____
- [ ] F-0052 Budget enforcement per Epic — no Stories → answer: ____
- [x] F-0053 Reviews return checks: every review ends in a machine-readable check table the tick reads — no Stories → answer: (spoken for: STARVED → PLAN)
- [x] F-0054 Failure classification before any relaunch: credential, quota, transport or work — no Stories → answer: (spoken for: CARD → SPEC)
- [x] F-0055 A frozen eval set for the factory's judging tools — no Stories → answer: (spoken for: CARD → SPEC)
- [x] F-0056 Memory to code, first pass: what a session has to remember becomes a script, a row or a rule — no Stories → answer: (spoken for: CARD → SPEC)
- [x] F-0060 A mechanical correctness pass before the human-standard review — no Stories → answer: (spoken for: CARD → SPEC)
- [x] F-0061 Hooks as rule enforcement inside every worker session — no Stories → answer: (spoken for: CARD → SPEC)
- [ ] F-0062 The launch path: skills as procedures, role agents with restricted tools, per-job caps, deny rules — no Stories → answer: ____
- [x] F-0063 Relaunch from the branch, not from zero — no Stories → answer: (spoken for: CARD → SPEC)
- [x] F-0064 Events at the source: every tool appends its own event, and the human log is rendered from the streams — no Stories → answer: (spoken for: CARD → SPEC)
- [x] F-0065 Spike: a sandboxed shell for worker sessions — no Stories → answer: (spoken for: CARD → SPEC)
- [x] F-0066 A progress heartbeat: no progress for twenty minutes is a stall, not only silence — no Stories → answer: (spoken for: CARD → SPEC)
- [x] F-0067 Clean floor, re-scoped: the sweep, the registry and the tool repository — no Stories → answer: (spoken for: CARD → SPEC)
- [ ] F-0068 Spike: one Feature through a hosted agent runtime, compared with the local pool — no Stories → answer: ____
- [ ] F-0069 Security checks by code: a review pass on sensitive paths, secret and dependency scanning as rules — no Stories → answer: ____
- [x] F-0070 A CI step-silence rule: a job with no new step for ten minutes is stalled, even under budget — no Stories → answer: (spoken for: CARD → SPEC)
- [ ] F-0075 Redaction is a gate on every write path: operator names and secrets never reach a record or a public repo — no Stories → answer: ____
- [ ] F-0080 Closing is derived and total: a definition of done per type, reconciliation for work that predates it, nothing re-emitted — no Stories → answer: ____
- [x] F-0084 The command surface reads as one product: names, one voice, and "writes" marked — no Stories → answer: (spoken for: CARD → SPEC)
- [ ] F-0091 asf improve: the self-improvement pass is a tick step, not something someone remembers to ask for — no Stories → answer: ____
- [ ] F-0092 An item has a budget: three sessions or $10, then it stops and says why — no Stories → answer: ____
- [ ] F-0093 Model routing is argued from minutes-per-landing, and the pool gets a cheap tier — no Stories → answer: ____
- [x] F-0094 A decided Epic with no Features never starts: no row breaks an Epic into Features — no Stories → answer: (spoken for: CARD → SPEC)
- [ ] F-0095 The wave relaunches coders on Tasks that end 'empty branch: nothing to land' — no Stories → answer: ____
- [ ] F-0096 The wave runs dry: 73 undecided cards and nothing moves them toward a spec — no Stories → answer: ____
- [ ] F-0097 asf status says nothing about Bugs and Features: add a Bugs row and a Features row — no Stories → answer: ____
- [ ] F-0098 'dead' sessions are nearly always finished ones not yet recorded: say 'ended, awaiting tick', free the slot at once — no Stories → answer: ____
- [ ] F-0099 Groom every tick: intake, policy pass and questions run on each tick, not once a day — no Stories → answer: ____
- [ ] F-0100 The savings pass: the tick proposes the next cheapest win from its own numbers — no Stories → answer: ____
- [ ] F-0101 Corrections of mechanical failures run on Opus: 25% of all spend goes to correct/adjudicate/groom sessions — no Stories → answer: ____
- [ ] F-0102 Deliveries: one plan, one agent, one gate for several small Features and Bug fixes across the backlog — no Stories → answer: ____
- [ ] F-0103 The tick files Bugs from its own session outcomes and a stalled wave — no Stories → answer: ____
- [x] F-0104 Products tick on a pinned live install; `asf upgrade` moves it to the merged head — no Stories → answer: (spoken for: CARD → SPEC)
- [x] F-0105 The landing gate runs 15–25 min on the operator's machine next to 8 sessions: gate on CI instead — no Stories → answer: (spoken for: CARD → SPEC)
- [x] F-0106 Plans cut Tasks already on trunk, and depend on ids that were never minted: coders end with an empty branch — no Stories → answer: (spoken for: CARD → SPEC)
- [x] F-0107 asf install: one command from zero to a ticking factory (from a release tag, doctor green) — no Stories → answer: (spoken for: CARD → SPEC)
- [ ] F-0108 Release ASF 0.1 for feedback: general installer, first-user docs, release notes, a feedback channel — no Stories → answer: ____
- [x] F-0109 install.sh: the operating session cannot run it, it leaves the pre-ASF clocks running, and it installs hooks without the human-now approval — no Stories → answer: (spoken for: CARD → SPEC)
- [x] F-0110 /asf:* in a product's own session shows another product: resolve the product from the working directory — no Stories → answer: (spoken for: CARD → SPEC)
- [x] F-0111 hooks install from asf-live tells the operator to wire the dev install's path into the product's git hooks — no Stories → answer: (spoken for: CARD → SPEC)
- [ ] F-0112 Products update themselves to a new ASF release: upgrade: auto in the tick, with rollback on a red doctor — no Stories → answer: ____
- [x] F-0113 An S1 silently holds all Feature work, and NEXT plans from different inputs than the tick — no Stories → answer: (spoken for: CARD → SPEC)
- [x] F-0114 Upgrades never break other installs: automatic, versioned, rollback-safe migrations with an upgrade test from every release — no Stories → answer: (spoken for: CARD → SPEC)
- [ ] F-0115 Worker sessions are refused the repo's own lint (check_conventions) and printenv — no Stories → answer: ____
- [x] F-0116 A session commits under its own identity, never the operator's — no Stories → answer: (spoken for: CARD → SPEC)
- [x] F-0117 Command center: follow every product's factory live and talk to each product's orchestrator; each product keeps its own controller — no Stories → answer: (spoken for: CARD → SPEC)
- [x] F-0118 Every product session shows its factory status each tick: a statusLine fed by the tick's snapshot — no Stories → answer: (spoken for: CARD → SPEC)
- [x] F-0119 asf pr-hygiene --lanes: a machine-readable listing product rule checks can call — no Stories → answer: (spoken for: CARD → SPEC)
- [x] F-0120 Adopting a record: flag cards that belong to another product and move or remove them in bulk (asf move) — no Stories → answer: (spoken for: CARD → SPEC)
- [x] F-0121 asf hooks install chains an existing git hook instead of refusing (stdin shared, one command) — no Stories → answer: (spoken for: CARD → SPEC)
- [x] F-0122 The /asf:* tables read backlog_dir, which the tick never pulls — views go stale — no Stories → answer: (spoken for: CARD → SPEC)
- [x] F-0123 record: a date-prefixed plan file mints no Tasks after it lands — no Stories → answer: (spoken for: CARD → SPEC)
- [x] F-0125 Onboarding: adopt a product's in-flight PRs (asf adopt-pr + legacy review form) so native landing drains them — no Stories → answer: (spoken for: CARD → SPEC)
- [x] F-0126 feeder: a Task with no writes: (migrated pre-ASF plan) is launched and can only block — no Stories → answer: (spoken for: CARD → SPEC)
- [ ] F-0127 A product declares a worktree setup command; the spawner runs it in every fresh worktree — no Stories → answer: ____
- [ ] F-0128 approvals: decide_feature: auto — ASF decides a product's Features itself (operator instruction) — no Stories → answer: ____
- [ ] F-0129 asf config check/migrate: an old or hand-made product yaml maps onto the schema without a hand rewrite — no Stories → answer: ____
- [ ] F-0130 backfill: reconcile a partly-built product's history into the record (asf backfill) — no Stories → answer: ____
- [ ] F-0131 Classify every cancelled CI job as timeout, runner loss or failure, and list jobs near their limit — no Stories → answer: ____
- [ ] F-0132 Derived backlinks copy a protected name from one card's title into others, and redaction then refuses unrelated pushes — no Stories → answer: ____
- [ ] F-0133 Groom commits its own output; a foreign daily stamp never skips the native daily; new products groom by default — no Stories → answer: ____
- [ ] F-0134 Intake silently ignores an explicit type: line — an operator's 'type: bug' card is minted as a Feature — no Stories → answer: ____
- [ ] F-0135 Native PR landing: merge an ASF-opened PR when approved and green, so PR products need no merge-queue script — no Stories → answer: ____
- [ ] F-0136 No command pauses a product's clock for a rollout — no Stories → answer: ____
- [ ] F-0137 Pause one product: the tick keeps recording and harvesting but launches nothing — no Stories → answer: ____
- [ ] F-0138 Predictive capacity: place a launch on the account that can finish it, by provider kind — no Stories → answer: ____
- [ ] F-0139 Status Record row: calculate the open split (work, Resolved, records) instead of one total — no Stories → answer: ____
- [ ] F-0140 The factory's own clock imports from the live dev checkout — a tick mid-update gets a torn tree; tick from a snapshot — no Stories → answer: ____
- [ ] F-0141 The landing gate refuses a branch that adds a debug-toggle marker — no Stories → answer: ____


## Stories without Tasks after plan-approved

(none)


## Landed before the id convention

(none)


## Merges proposed

- [ ] T-0073 merge T-0073+T-0074 — F-0091: writes overlap (docs/products.example.yaml ↔ docs/products.example.yaml); they run one after the other anyway, one session and one gate saved → answer: ____
- [x] T-0107 merge T-0107+T-0108+T-0109 — F-0024: writes overlap (tests/test_amendable.py ↔ tests/test_amendable.py); they run one after the other anyway, one session and one gate saved → answer: (spoken for: PLAN → CODE)
- [ ] T-0112 merge T-0112+T-0113+T-0114+T-0115 — F-0025: writes overlap (tests/test_lifecycle.py ↔ tests/test_lifecycle.py); they run one after the other anyway, one session and one gate saved → answer: ____
- [ ] T-0119 merge T-0119+T-0120+T-0121+T-0122 — F-0039: writes overlap (tests/test_continuation.py ↔ tests/test_continuation.py); they run one after the other anyway, one session and one gate saved → answer: ____
- [ ] T-0137 merge T-0137+T-0138+T-0139 — F-0030: writes overlap (tests/test_readme.py ↔ tests/test_readme.py); they run one after the other anyway, one session and one gate saved → answer: ____
- [ ] T-0153 merge T-0153+T-0154+T-0155+T-0156 — F-0097: writes overlap (asf/views/work.py ↔ asf/views/work.py); they run one after the other anyway, one session and one gate saved → answer: ____
- [ ] T-0159 merge T-0159+T-0161+T-0162+T-0164 — F-0100: writes overlap (docs/products.example.yaml ↔ docs/products.example.yaml); they run one after the other anyway, one session and one gate saved → answer: ____
- [x] T-0165 merge T-0165+T-0167 — F-0101: writes overlap (asf/briefs/build.py ↔ asf/briefs/build.py); they run one after the other anyway, one session and one gate saved → answer: (spoken for: PLAN → CODE)
- [x] T-0218 merge T-0218+T-0219 — F-0043: writes overlap (asf/tick/file_bugs.py ↔ asf/tick/file_bugs.py); they run one after the other anyway, one session and one gate saved → answer: (spoken for: PLAN → CODE)


## Batches proposed

- [ ] T-0144 batch T-0144+T-0146 — F-0038 has 4 ready Tasks for 2 slots; these 2 write ≤2 globs each: one session and one gate instead of 2 → answer: ____
- [x] T-0194 batch T-0194+T-0196+T-0197+T-0198+T-0199 — F-0040 has 7 ready Tasks for 2 slots; these 5 write ≤2 globs each: one session and one gate instead of 5 → answer: (spoken for: PLAN → CODE)
- [ ] T-0173 batch T-0173+T-0174+T-0177 — F-0102 has 4 ready Tasks for 2 slots; these 3 write ≤2 globs each: one session and one gate instead of 3 → answer: ____


## Splits proposed

- [ ] T-0161 split asf/savings | tests — tests is held by T-0160 (Active); asf/savings is free: the free part can start now → answer: ____
- [x] T-0180 split tests | tests/fixtures — tests is held by T-0122 (ready, F-0039); tests/fixtures is free: the free part can start now → answer: (spoken for: PLAN → CODE)
- [ ] T-0189 split asf/tick | sample | docs | tests | asf | rules — asf/tick is held by T-0203 (ready, F-0103); sample is held by T-0122 (ready, F-0039); docs is held by T-0185 (ready, F-0093); tests is held by T-0085 (Active); asf is held by T-0056 (Active); rules is free: the free part can start now → answer: ____


## Split parts to confirm

(none)


## Blocked on a Closed item

(none)


## Near-duplicate titles

(none)


## Undecided > 14 days

(none)


## Auto-filed Bugs not yet decided

- [ ] B-0102 asf groom / asf set write the checkout and never push — edits drift from the tick clone — auto-filed, count 1 → answer: ____
- [ ] B-0103 asf init writes a pre-commit that doctor then calls foreign — a new product starts RED — auto-filed, count 1 → answer: ____
- [ ] B-0104 briefs: spec/plan templates don't state the commit-subject rule harvest enforces — auto-filed, count 1 → answer: ____
- [ ] B-0105 Config reader: a YAML flow-style map loads silently as a string — parse it or reject it clearly — auto-filed, count 1 → answer: ____
- [ ] B-0106 Failed tick step logs no traceback — auto-filed, count 1 → answer: ____
- [ ] B-0107 feeder: a pushed plan/spec awaiting its PR is relaunched as STARVED → PLAN/SPEC — auto-filed, count 1 → answer: ____
- [ ] B-0108 Flow-style config maps load as strings; readers silently fall back — auto-filed, count 1 → answer: ____
- [ ] B-0109 Full test suites run locally in parallel and exhaust the host (load 99, swap 95%) — auto-filed, count 1 → answer: ____
- [ ] B-0110 Harvest re-gates a green code branch when main moved only by docs commits — auto-filed, count 1 → answer: ____
- [ ] B-0111 Inbox intake drops headers after an unknown key; a Bug card became a Feature — auto-filed, count 1 → answer: ____
- [ ] B-0112 install.sh reports step 4 FAILED for scheduler.kind cron, which is the expected outcome — auto-filed, count 1 → answer: ____
- [ ] B-0113 install.sh step 1 failure exits with pipx's code and no NEEDS OPERATOR guidance — auto-filed, count 1 → answer: ____
- [ ] B-0114 native landing: a merged spec/plan PR marks its Feature landed/Resolved, so no coder ever launches — auto-filed, count 1 → answer: ____
- [ ] B-0115 scheduler install runs the daily clock at load, hours outside its declared time — auto-filed, count 1 → answer: ____
- [ ] B-0116 Spec/plan approval ignores review_pattern: one review-file contract for every kind — auto-filed, count 1 → answer: ____
- [ ] B-0117 Status Prod row names ci.deploy_workflow but the product schema rejects it — auto-filed, count 1 → answer: ____
- [ ] B-0118 status: the Prod row reads ci.deploy_workflow, not deploy_sha.workflow — auto-filed, count 1 → answer: ____
- [ ] B-0119 tick logs stay empty while steps run; a live tick looks the same as a hung one — auto-filed, count 1 → answer: ____
- [ ] B-0120 Invariant I10 refused a write to features/F-0095.md — auto-filed, count 1 → answer: ____


## Inbox cards with a question

- [ ] inbox:32-pre-existing-i3-writes-overlaps-after-the-invariants-audit-groom-on-no-clock.md 32 pre-existing I3 writes: overlaps after the invariants audit; groom on no clock — Which Epic is this under? No open Epic shares a title word with it. → answer: ____
- [ ] inbox:a-push-refused-by-the-repo-s-pre-push-hook-loops-as-unpushed-work-carry-the-hook-s-output-into-the-correction-and-route-by-it.md A push refused by the repo's pre-push hook loops as 'unpushed work' — carry the hook's output into the correction and route by it — This reads as a defect. A Bug carries a signature — add signature: <the failing test or error line>; or an ## Acceptance list if it is new work. → answer: ____
- [ ] inbox:a-removed-or-moved-feature-still-shows-a-derived-stage-plan-approved-in-index-and-tables.md A removed or moved Feature still shows a derived stage (plan-approved) in index and tables — Which Epic is this under? No open Epic shares a title word with it. → answer: ____
- [ ] inbox:asf-collects-trunk-ci-runs-itself-ci-facts-go-stale-when-a-product-s-legacy-collector-is-retired.md ASF collects trunk CI runs itself — CI facts go stale when a product's legacy collector is retired — This reads as a defect. A Bug carries a signature — add signature: <the failing test or error line>; or an ## Acceptance list if it is new work. → answer: ____
- [ ] inbox:connectors-declare-each-external-service-ci-deploy-db-once-per-product-asf-connect-sets-up-auth-doctor-status-approvals-sessions-all-read-it.md Connectors: declare each external service (CI, deploy, db, …) once per product — asf connect sets up auth, doctor/status/approvals/sessions all read it — This reads as a defect. A Bug carries a signature — add signature: <the failing test or error line>; or an ## Acceptance list if it is new work. → answer: ____
- [ ] inbox:conventions-doc-paths-let-a-product-declare-extra-document-folders-so-approved-spec-branches-land-without-a-session.md conventions.doc_paths: let a product declare extra document folders so approved spec branches land without a session — Which Epic is this under? No open Epic shares a title word with it. → answer: ____
- [ ] inbox:conventions-shared-paths-lock-files-overlap-every-task-serialise-them-at-merge-not-in-the-feeder.md conventions.shared_paths: lock files overlap every Task — serialise them at merge, not in the feeder — Which Epic is this under? No open Epic shares a title word with it. → answer: ____
- [ ] inbox:coverage-a-product-declares-references-generic-reference-coverage-generalises-matrix-path-asf-parity.md coverage: a product declares references; generic reference coverage (generalises matrix_path + asf parity) — This reads as a defect. A Bug carries a signature — add signature: <the failing test or error line>; or an ## Acceptance list if it is new work. → answer: ____
- [ ] inbox:done-table-credits-landed-sha-to-a-run-with-no-commits-of-its-own.md DONE table credits 'landed <sha>' to a run with no commits of its own — Which Epic is this under? No open Epic shares a title word with it. → answer: ____
- [ ] inbox:groom-digest-for-you-fills-with-rule-decidable-noise-379-on-the-first-customer-only-human-now-actions-belong-there.md Groom digest 'For you' fills with rule-decidable noise (379 on the first customer) — only human-now actions belong there — Which Epic is this under? No open Epic shares a title word with it. → answer: ____
- [ ] inbox:one-factory-flags-the-asf-repo-s-own-agent-worktrees-as-a-second-factory.md one-factory flags the ASF repo's own agent worktrees as a second factory — This reads as a defect. A Bug carries a signature — add signature: <the failing test or error line>; or an ## Acceptance list if it is new work. → answer: ____
- [ ] inbox:session-model-per-class-within-a-kind-e-g-s2-fix-bug-reviews-on-the-light-model.md Session model per class within a kind (e.g. S2 fix-bug reviews on the light model) — Which Epic is this under? No open Epic shares a title word with it. → answer: ____
- [ ] inbox:one-factory-is-always-red-for-the-asf-product-its-repo-is-the-factory-source.md one-factory is always RED for the asf product: its repo is the factory source — This reads as a defect. A Bug carries a signature — add signature: <the failing test or error line>; or an ## Acceptance list if it is new work. → answer: ____
- [ ] inbox:the-factory-installs-its-own-trunk-a-landed-fix-is-live-on-the-next-tick.md The factory installs its own trunk: a landed fix is live on the next tick — This reads as a defect. A Bug carries a signature — add signature: <the failing test or error line>; or an ## Acceptance list if it is new work. → answer: ____
- [ ] inbox:landed-spec-plan-branches-keep-their-worker-worktree.md Landed spec/plan branches keep their worker worktree — Which Epic is this under? No open Epic shares a title word with it. → answer: ____
