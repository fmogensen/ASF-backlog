# Groom — 2026-09-21

The first groom of this record, written by hand because the groom pass is part of the package that
does not run here yet. Answer one word per line where a line asks for one: `yes` / `no` /
`rank <n>` / `close` / `merge into <id>`.

## Near-duplicate titles

The migration carried three passes of one job across as three separate Features, because a Story's
parent must be a Feature and each pass minted its own. They are the same work.

- [ ] [[F-0056]] Memory to code, first pass — merge into one card?
- [ ] [[F-0057]] Memory to code, second pass — merge into [[F-0056]]?
- [ ] [[F-0058]] Memory to code, third pass — merge into [[F-0056]]?

The clean-floor work arrived twice: once as the original Feature and once as the card that
re-scoped it.

- [ ] [[F-0049]] The factory floor is always clean — keep both, or fold [[F-0067]] into it?
- [ ] [[F-0067]] Clean floor, re-scoped — fold into [[F-0049]]?

## Undecided cards

Every one of these is `decided: false` and gets no work until it is answered.

- [ ] [[E-0002]] ASF 0.2 — self-improvement (undecided on purpose: it is gated on 0.1's data)
- [ ] [[E-0003]] ASF 0.3 — autonomous delivery (same)
- [ ] [[F-0047]] The factory's self-improvement loop — decided, or held for the 0.2 Epic?
- [ ] [[F-0050]] The product improvement loop — decided, or held?
- [ ] [[F-0052]] Budget enforcement per Epic — priority `nice`; decide or close?
- [ ] [[F-0068]] Spike: a hosted agent runtime — held behind a measured trigger; leave undecided?

## Questions this seed could not answer

- [ ] **The rank scheme.** Features 1–21 are the build phases in plan order, 22–30 the cards carried
  from the prototype, 31–33 the three gaps the plan named, 34–70 the migrated cards in their source
  order. The plan ranks the prototype cards highest *by value* — the preamble first, on cost. If
  the roadmap should read value-first rather than phase-first, the two blocks swap. Answer:
  `phases first` or `value first`.
- [ ] **The id scheme.** Fresh numbering from 1 per type, no link back to the private record, the
  old id only in the migration map. Confirm before the first push, because after it the ids are
  public.
- [ ] **The product config shape.** [[B-0003]]: the operator's product file for this product nests
  its keys where the config reader expects them flat, and loading it silently yields a product with
  no repository and no record. Which shape is canonical — the reader's, or the file's?
- [ ] **The rule set.** All 142 migrated rules are `enforced: false` with `reason: check to be
  ported`; none of the source check scripts is generic as it stands. Curating them down to a core
  set that ships with the package is [[F-0012]] (P3 wave 4). Nothing here decides which of the 142
  survive that.
- [ ] **The constitution's wording.** `docs/CONSTITUTION.md` is a draft. The evidence and the card
  references should survive editing; the sentences are the operator's.

## Notes

- No `moved_from` is written on any migrated card, and no card names the record it came from: that
  record is private. The controller marks the old cards `moved_to` from the migration map.
- Seven Bugs ([[B-0001]]–[[B-0007]]) are gaps in `asf init` found by laying this record down by
  hand. They are the dogfood result of P0c.
